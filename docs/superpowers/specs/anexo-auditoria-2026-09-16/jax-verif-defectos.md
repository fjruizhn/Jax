**Defectos**
- D1 · PARCIAL · media. Los cortes sin redactar están donde dice: `jacobs/executor.py:386,448`, `jax/muscles/base.py:264,306` (el informe dice :305) y `ollama_muscle.py:119`. El contrato de `jax/core/redaccion.py:161-167` también está bien citado. Pero la frase "a jacobs_steps.error sin redactar" es falsa: `_fail_step` (`executor.py:1270`) pasa todo por `redactar_secretos()` antes de guardarlo en jacobs_steps.error y en los eventos STEP_FAILED/PIPELINE_ABORTED. En el REPL, `humanizar_error` (`jax/core/main.py:161`) también redacta. Lo que de verdad falla: (a) se corta antes de redactar y no se pasa la credencial conocida, así que un secreto partido en el carácter 200 o una key sin forma reconocible se escapa; (b) `main.py:487` guarda `str(e)` sin redactar en el archivo de resultado de `run_task`; (c) el informe no ve `jacobs/plan.py:655`: `body[:200]!r` de Ada va a `logger.error` y a `CerebroNoDisponible` sin redactar. Detalle menor: la línea de Gemini es la 293, no la 292. Arreglo: cambiar todos los `[:200]` por `recortar_redactado(texto, 200, [credencial])` (incluidos `plan.py:655` y `main.py:487`) y agregar un test que busque `[:N]` sobre `resp.text`/`body`.
- D2 · VERDADERO · baja. `policy.py:48` solo mira que el token no esté vacío, y `git grep subpipeline_token` no encuentra otra validación (`models.py:117`, `routes.py:152`). La severidad es baja por tres motivos. El router de Jacobs no tiene autenticación, pero LAS MANOS escucha solo en 127.0.0.1 (`/etc/systemd/system/jax-las-manos.service:13`, `config.toml:13`). Cualquier proceso local puede mandar `invoked_by="jax_local"`, que está en `VALID_INVOKERS` (`models.py:56`) y no pide token, así que el token no protege ningún privilegio. Y jax-platform impone `invoked_by="plataforma"` (`backend/api/pipelines.py:125,190`). Arreglo: quitar el token y `ada` de `VALID_INVOKERS` hasta que exista un emisor, o emitir tokens de un solo uso del lado del servidor (patrón `HumanGate`, `server.py:98-128`). Es un contrato de autoridad, así que la decisión es de Fernando.
- D3 · VERDADERO · baja. `routes.py:147-153` no pasa `subpipeline_depth` y `git grep subpipeline_depth` solo encuentra `policy.py`. Hoy nada crea sub-pipelines, así que es un freno muerto, no algo explotable. Arreglo: tomar la profundidad del registro del pipeline padre en el servidor (nunca del cuerpo del pedido), o borrar el límite y el token juntos.
- D4 · VERDADERO · media. En `plan.py:799-800` la faceta desconocida pasa a `jax_local` sin log ni evento, y la capability en `:815-819` sí avisa. "Choca con P10" es discutible: P10 (`policy/rules/P10-fail-open-prohibido.yaml`) trata de excepciones que fallan abiertas, aunque el espíritu de "no degradar en silencio" sí aplica. Arreglo: rechazar el paso o el plan (como `CapabilityUnbound`), o como mínimo `logger.warning` más un evento, y validar la faceta contra la tabla `facet` en vez de `VALID_FACETS`.
- D5 · VERDADERO · baja. `executor.py:1067` dice "nadie lee ese .md", pero `jax-platform/backend/api/admin/repository.py:10-11,33-45` lista `~/jax/repo/documents` y sirve sus archivos (router montado en `main.py:148`). El `except` sigue siendo tolerable (el resultado canónico ya quedó guardado), pero la justificación es falsa. Arreglo: reescribir la marca con la razón verdadera (copia de cortesía que el admin puede ver; el resultado canónico está en `output_ref`).
- D6 · VERDADERO · baja. `_safe_path` (`repository.py:14-19`) usa `startswith(base)` sin separador, y `get_file` solo revisa `parts[0]`, así que `documents/../../repo-x/f` pasa la guarda. Solo llega un superadmin y hoy no existe ningún `~/jax/repo*` hermano (`ls -d ~/jax/repo*` devuelve solo `repo`). Arreglo: `os.path.commonpath([base, target]) == base` (o `Path.is_relative_to`).
- D7 · VERDADERO · baja-media. `requirements.txt` no trae ni `cryptography` ni `pyyaml`, y `README.md:72` indica instalar desde ese archivo. Producción funciona porque los venvs se armaron a mano: `las_manos/.venv` tiene cryptography y `.venv` tiene cryptography y pyyaml. CI instala a mano en cada job (`.github/workflows/policy.yml:212,321,374,504,561`). Con un clon nuevo, credential_resolver y la gobernanza no importan. Arreglo: fijar `cryptography==49.0.0` y `pyyaml==6.0.3` en `requirements.txt` y que CI instale desde ese archivo.

**No tocar sin decisión de Fernando**
- N1 · VERDADERO; mandarlo a Fernando fue correcto. `hyde_requires_human_gate` aparece una sola vez (`policy.py:96`) y el gate real está en `store.py:509-538`. Es un candado de autoridad.
- N2 · VERDADERO; correcto. `credential_resolver.py:29-39,124-139` está bien citado. El timer dice 2026-08-16 en la descripción y tiene `OnCalendar=2026-08-26`. Ni `ops/b14-exit-criterion.log` ni ningún otro log B14 existe en `ops/`, solo el `.sh`.
- N3 · VERDADERO; correcto (la decisión está documentada en `crypto_secrets.py:19-37`). Nota: `las_manos/crypto_secrets.py` es una tercera copia en un archivo normal, no un symlink.
- N4 · VERDADERO; correcto (`usage_writer.py:7-13` documenta que es copia a propósito).
- N5 · VERDADERO; correcto. Solo lo usan `las_manos/facet_client.py` y `tests/test_thot_connection.py`.
- N6 · PARCIAL; enviarlo a Fernando fue correcto. Los dos `.md` están en git, pero `_director_patch/` también tiene 4 archivos `.py` en git (`executor_block.py`, `routes_block.py`, `plan_and_relaunch.py`, `test_jacobs_director.py`) que la ficha no menciona.
- N7 · VERDADERO; correcto. `git status --ignored` muestra 25 backups (14 en `jacobs`, 11 en `las_manos`), casi todos del 2026-08-21 y uno del 08-27.

**regla:**
- R1 · VERDADERO, con números de línea algo corridos. `executor.py:44-45`, `plan.py:46`, `db.py:487` y `ollama_muscle.py:62` son exactos. La URL de OpenAI está en `base.py:284`, no en la 285.
- R2 · VERDADERO. `tts.py:35`, `executor.py:1224`, `policy.py:14` y `config.toml:17` (`kill_switch_path`) confirman las dos fuentes.
- R3 · VERDADERO. `plan.py:81-83`; el fallback del REPL está en `jax/core/router.py:59-63`.
- R4 · PARCIAL. Es cierto, pero las líneas están mal y la lista está incompleta. Los clientes están en `executor.py:287` (no 285), `383` y `445`, y en `base.py:260` y `301` (no 300). Faltan `executor.py:577,598,656`, `base.py:412` y el cliente de `plan.py:~749`.

**Totales.** Defectos: 6 verdaderos y 1 parcial (D1). No tocar: 6 verdaderos y 1 parcial (N6), y los 7 estaban bien mandados a Fernando. regla: 2 verdaderos, 2 con línea corrida o lista incompleta (R1 y R4 marcado parcial) — en la práctica 3 verdaderos y 1 parcial. En total: 16 verdaderos, 3 parciales y 0 falsos.