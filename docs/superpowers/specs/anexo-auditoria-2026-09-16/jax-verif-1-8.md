1. VALID — Nothing imports `_director_patch`. It only shows up in 4 `_NO_PARSEA` tables (policy/tests/test_no_fail_open_except.py:74, tests/test_aiomysql_connect_timeout_tripwire.py:83, tests/test_no_blocking_in_async.py:40, tests/test_payload_max_tokens_literal_tripwire.py:69), in the comment at policy.yml:734, and in docs. The code is already in the live files (jacobs/executor.py:980 and :1019, jacobs/plan.py:159 and :243). `git -C jax-platform grep` finds only a doc that calls it out of scope. No systemd unit or script uses it. CI runs `tests/test_jacobs_director.py` (policy.yml:566/601), a different file from `_director_patch/test_jacobs_director.py` (cmp: they differ). The scan has 209 .py files, so dropping 4 leaves 205, still above the floor of 170. The existence checks are real: test_aiomysql…:425-426, test_no_blocking…:189-191, test_no_fail_open…:339-341. The ficha's plan to rebuild them with a tmpdir file is needed: with an empty `_NO_PARSEA` those loops pass without checking anything.
2. VALID — `git grep StepResult` finds only the definition at jacobs/models.py:155. jax-platform has 0 hits.
3. PARTIAL — The copy at jacobs/models.py:47 really is unused; the only reader is plan.py:799, which uses its own copy at plan.py:81. But "no written reason" and "jax-platform → 0" are wrong. jax-platform/docs/fase2-facetas-diseno.md (C1.4, rows 6 and 7) marks both lists ELIMINAR (#6 → derive from `facet` via `facet_resolver`). Row 7 says to delete the plan.py copy and import from models.py, the opposite of the ficha. Correct fix: keep the models.py copy (or derive from the `facet` table) and delete plan.py:81 as the design says. Deleting the models.py copy works, but it goes against that plan and keeps the hardcoded list in the planner.
4. VALID — `TRAFFIC_CLASSES` has 1 hit in jax and 0 in jax-platform. Its own comment says it is "documentados para referencia" and that audit.py does not validate them (las_manos/audit.py:40-49). The enforced list is the `Literal` at las_manos/envelope.py:75-78, with the same 6 values.
5. VALID — `enabled_motors` appears only at las_manos/motor_registry/catalog.py:181, plus a mention in a plan doc. jax-platform has 0 hits.
6. VALID — The 4 tracked files (2×~63 MB, 121M in total) have no reference in .py/.toml/.sh/.md, in `jax/voice/kokoro_worker.py`, in /etc/systemd or ~/.config/systemd, in ops/scripts/.github, or in jax-platform. Voice uses Kokoro (jax/voice/tts.py:35) with voice_ids such as `em_alex` (config/config.toml:32). The files stay in git history, so they can be recovered.
7. FALSE — The ficha says `git -C jax-platform grep cleanup.sh` returns 0. It does not: jax-platform/backend/jax_engine/owner_cleanup.py:5-11 names `~/jax/scripts/cleanup.sh` as the external retention script for `~/jax/missions`, and builds its reaper around it (the 30-day `COMMAND_OWNER_MAX_AGE_SECONDS` fallback). jax-platform/backend/api/command.py:33-34 and jax/core/main.py:412 still write `web-task-*.md` and `_result.md` files there, and nothing else deletes them. Deleting the script breaks nothing at runtime, since it has no scheduler and the reaper has its own fallback. Correct fix: delete it and, in the same change, update that jax-platform docstring. If mission files need retention, record who does it now.
8. VALID — I checked every TOML reader:
   - las_manos/server.py:71-80 reads server, human_gate, ops, facets and environments.
   - las_manos/motor_registry/routes.py:44/54 reads only `server.kill_switch_path`.
   - policy/governance/validator.py:96-100 reads only `ops`.
   - jacobs/executor.py:69, jax/core/main.py, jax-platform api/chat.py:504 and the probe tests read config/config.toml, a different file.
   - The only `config.get("motors")` is at catalog.py:125, fed by dicts built by hand in tests.
   - Only `render_templates.yaml` has a pinned hash (loaders.py:53-70), and tests/test_governance_validator.py:148 recomputes the config.toml hash when it runs.
   - The jax-platform cache is keyed on mtime, so it rebuilds after the edit.

   The required follow-up is correct: the "Fuente" line in closed_vocabulary.yaml:68 must point to the DB `motor` table. The mentions in migrations.py:663 and CONTEXT.md:278 are history only.

Counts: VALID 6 (1, 2, 4, 5, 6, 8) · PARTIAL 1 (3) · FALSE 1 (7) · HARMFUL 0