9. VALID. I parsed the files with Python's AST parser, and all 10 imported names are unused. Each name appears only on its own import line; `FacetUnavailableError` shows up a second time only in a comment (executor.py:939). None of the files defines `__all__`. Nothing imports these names from these modules, and no `patch`/`monkeypatch.setattr` targets them there: the tests patch `worker.resolve_credential_instrumented` and `base.resolve_credential_instrumented`. The ficha is right that `credential_resolver` still gets loaded, because facet_resolver.py:22 imports it first.

10. VALID. The two `crypto_secrets.py` files are byte-identical, and the file imports nothing from `jax.*`. There are 8 symlinks under `las_manos/` today and the tests already check some of them with `is_symlink` (e.g. test_redaccion.py:30). The mirror checker has no symlink handling at all, so the `las_manos` comparison simply does nothing. `rglob` still lists the symlinked file, so the scanner's file count (`PISO_ARCHIVOS_ESCANEADOS`) stays the same. Nothing refers to `las_manos/crypto_secrets.py` by path, in jax or in jax-platform. Only the `nota` and the comment in check_mirror_sync.py:196 need rewording.

11. PARTIAL. The diagnosis is right: the diffs are only the import lines and the wording of the `# fail-soft:` comment, and `model_catalog` is not in `FAMILIAS`. The remedy also works, since the CI jobs L322/L375 use `PYTHONPATH=las_manos`, where the bare import succeeds. One claim is false: `test_no_fail_open_except` does not dedupe by `path.resolve()`. Line 85 is inside `_declarado_no_parsea`, and `find_fail_open_excepts` (L194-208) scans every path `rglob` returns. The symlink is therefore scanned under both paths, which is harmless because both show the same marker. The ficha also leaves out one side effect. In `.:las_manos` contexts, `jax.core.credential_resolver` would switch to the bare `crypto_secrets` and `db_connect_config` modules. No test patches `jax.core.crypto_secrets`, so this is harmless too, but it should be stated. Two more places need updating: the docstring at jax/core/db_connect_config.py:31-32 (it names these files as real copies) and policy/vocabulary/closed_vocabulary.yaml:72.

12. VALID. `aiofiles` appears only at executor.py:1221/1257, requirements.txt:1 and policy.yml:557-561, and nothing in the jax `.venv` depends on it. `asyncio.to_thread(filepath.write_text, …)` writes the same content and raises the same OSError. test_no_blocking_in_async only looks at call names, so passing the method by reference is not flagged. test_hipatia_fuentes.py:204-213 still covers the function. Two small gaps: the CI comment on L557-558 has to change, and `repo_dir.mkdir` stays a blocking call inside the async function. It could go into the same thread call.

13. PARTIAL. The duplicated literal is real, but the first check is at routes.py:101, not :100. The headline remedy of importing `MAX_STEPS_PER_PIPELINE` from `jacobs/policy.py` into models.py would create a circular import, because policy.py:12 already imports `jacobs.models`. The correct fix is to define the constant in `jacobs/models.py`, have policy.py import it from there, and use it in routes.py:101 and models.py:129. The error messages "(20)" and "entre 1 y 20" must then read the value from the constant. Nothing in jax tests or jax-platform depends on the 20.

14. VALID. db.py:484 matches the ficha. For any object that can be sliced, `text[:4000]` gives the same result. If `text` is `None`, both versions raise `TypeError`, which the marked `except` at L499 catches either way.

15. VALID. The filter is identical at base.py:271-272 and :337-338, and in both methods the result then gets `"\n".join(...).strip()`. In the DeepSeek method, `record_resolved_version_safe` runs between building the lines and joining them. The filter has no side effects, so the order doesn't matter. The filter has no copy in jax-platform or in the mirror checker. Side note: the `"⚙️ *Origen"` string is a hardcoded copy of the Kimi `authority_origin` in config/config.toml:335. The new helper would be a good place to read it from config instead.

Counts: VALID 5 (9, 10, 12, 14, 15) · PARTIAL 2 (11, 13) · FALSE 0 · HARMFUL 0