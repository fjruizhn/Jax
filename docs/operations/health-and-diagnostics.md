# Health and diagnostics

`jaxctl health` is an operational reachability diagnostic, not proof of authority or evidence integrity. `jaxctl status` aggregates live queries and is not persisted truth. Where a health endpoint is absent, it is **ABSENT**; use the relevant runbook/manual diagnostic rather than inventing a green status.
