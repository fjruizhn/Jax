# 2026-09-20: produccion deja de servir desde el checkout de trabajo de un
# agente (/home/fruiz/jax, que ese dia estaba en la rama
# phase1/pure-authority-resolver). Reversible: borrar este archivo y
# 'systemctl daemon-reload' devuelve todo al estado anterior.
[Service]
WorkingDirectory=/srv/jax-prod/jax/las_manos
ExecStartPre=/usr/local/sbin/jax-checkout-de-produccion-sano.sh /srv/jax-prod/jax
ExecStart=
ExecStart=/srv/jax-prod/jax/las_manos/.venv/bin/uvicorn server:app --host 127.0.0.1 --port 7777
