# CONTEXT.md — JAX 2.0 (Biblioteca de Alejandría)

> Documento canónico del proyecto JAX. Última actualización: **20 de agosto de 2026** (pago de deuda, ronda 2 — T1 presupuesto de facet INVESTIGADO Y DETENIDO EN GATE explícito, no implementado; T2 tres columnas menores CERRADAS con veredicto; T3 diseño de las 5 tablas de memoria entregado, sin implementar). Previa: 19-ago (pago de deuda post-GAP2 — scanner de columnas sin consumidor + fail-open de output_validator cerrado; T3/T4 deferidos a sesión propia). Previas: 19-ago (GAP2 Fase4 — bucle multi-turno de tool-calling con cotas, en master); 19-ago (GAP2 Fase2 — gate de autoridad tool→capability + read_file ejecutable, en master); 9-ago (Fase 2 Bloque D — catálogo de modelos y sincronización, diseñado e implementado; rama `infra/facetas-bloque-d`, no mergeada a master). Previas: 9-ago (MariaDB 11.8→12.3 LTS + Fase 1 credenciales a la DB, rama `infra/mariadb-12.3-migration`, no mergeada); 30-jun (Jacobs Director de Orquesta (wave scheduler) + contrato de capabilities cerrado de raíz en producción/master; AUTONOMIA_ANTIERROR.md creado (Fase A hecha)); 18-jun (Thot cerrado — GPT-5.5 operativo; Ada bautizada — GLM-5.2, sexta faceta, arquitecta de código, key Z.ai pendiente semana 22-jun; Hefesto/Kimi y Cassandra/Grok definidos como motores futuros por la Mesa); 14-jun (LAS MANOS, kill switch en vuelo); 4-jun (hilo compartido + qwen2.5:7b + VOZ Kokoro + clasificador local). ⚠️ §2/§3/§8 siguen desactualizados (hardware viejo, conteo de facetas, Jacobs v0.3) — pendiente reescribir con cabeza fresca, ver nota en la entrada del 30-jun.
> Regla de la casa: **NO SUPONER. Medir dos veces, cortar después.** Si algo de este documento contradice la realidad del servidor, gana la realidad — y se corrige este documento.

## 1. Qué es JAX

JAX (se pronuncia **"Yax"**, J española) es el asistente personal y futura plataforma de orquestación de Fernando Ruiz. Un solo ser con **7 facetas operativas** (jax_local, jekyll, hyde, hipatia, thot, kimi, ada — no 7 asistentes). Núcleo orquestador propio en **Python puro + asyncio**, sin frameworks. Honra la memoria de **Jairo Urbina**, amigo, socio y pionero del software libre en Honduras — su espíritu va en el system prompt de cada faceta. Easter egg: `IDE1990`.

Visión: JAX es base y laboratorio de un producto comercial globalmente escalable (multi-AI, multi-usuario, pricing por niveles) y fundación futura para AteneaERP y HAMMURABI.

## 2. Hardware y entorno (hall9000)

- **hall9000** (172.16.20.5): Ryzen 5 8500G, 30GB RAM, GPU AMD RX 9060 XT 16GB vía **Vulkan** (RADV GFX1200; ROCm inmaduro para gfx1200, diferido). Ubuntu 24.04.4. SSH puerto **58292** (verificado jun-2026; las notas viejas decían 58291).
- Proyecto: `~/jax/`, venv `~/jax/.venv` (Python 3.12; httpx, pydantic; **sin torch**).
- Credenciales: `/etc/jax/.env` (chmod 600): DEEPSEEK_API_KEY, GEMINI_API_KEY, OPENAI_API_KEY, KIMI_API_KEY, JAX_DB_*. ZHIPU_API_KEY pendiente (Z.ai abre API semana del 22-jun).
- Kill Switch: `/etc/jax/PAUSE` (si existe, JAX no invoca músculos).
- Audio: parlantes en `plughw:2,0` (card 2, ALC897 Analog, conector verde).
- Arranque: `cd ~/jax && set -a; source /etc/jax/.env; set +a && PYTHONPATH=. .venv/bin/python -m jax.core.main`

## 3. Las 7 facetas (estado real)

| Faceta | Icono | Músculo | Modelo | Rol | Voz |
|---|---|---|---|---|---|
| JAX local | 🏠 | OllamaMuscle (GPU local) | **qwen3:14b** (actualizado jun-2026; GPU local vía Vulkan) | Conversación cotidiana, privada. Tono hondureño **sobrio**: "maje" con medida, sin groserías, sin modismos ajenos | em_alex @1.0 |
| Hyde | 🔧 | SubprocessMuscle (`claude -p`) | sonnet | Técnico: código, infra | em_santa @1.0 |
| Jekyll | 🧠 | HttpMuscle (DeepSeek) | deepseek-chat | Humanista erudito, español neutro formal | em_santa @0.85 |
| Hipatia | 🔍 | HttpMuscle (Gemini + grounding) | gemini | Investigación con fuentes web | ef_dora @1.0 |
| Thot | 📜 | HttpMuscle (OpenAI) | gpt-5.5 | Crítico: abogado del diablo, guardián del largo plazo | em_alex @0.9 |
| Kimi | ⚙️ | HttpMuscle (Moonshot AI) | kimi-k2.7-code | Motor de enjambre: coding agéntico, refactors amplios, exploración paralela. Subordinado a Ada/Hyde. | em_alex @1.0 |
| Ada | 🏛️ | HttpMuscle (Z.ai) | glm-5.2 | Arquitecta de código: 1M contexto, largo horizonte. Nombrada en honor a Ada Lovelace. **Pendiente key Z.ai (semana 22-jun)** | ef_dora @1.0 |

Notas: llama3.2:3b sigue en `models_allowed` (tareas mecánicas rápidas). qwen2.5:14b permitido, no medido. ⚠️ Gemini en plan gratuito: **rate limits frecuentes** (Hipatia se quedó sin tokens 2 veces el 3-4 jun) — pendiente subir tier o espaciar uso. ⚠️ Jekyll **no tiene búsqueda web**: jamás usarlo como investigador (alucina con confianza; lección aprendida — recomendó config TTS inexistente).

## 4. Arquitectura del código (`~/jax/jax/`)

- `core/main.py` — REPL. Input vía **run_in_executor** (no congela el event loop; la voz suena de fondo). Comandos: `/voz on|off`, `/callate`, `/fact ...`, `salir`. Hilo de conversación en RAM + guardado MariaDB + voz como task de fondo. Kill switch antes de cada invoke. Errores humanizados.
- `core/router.py` — Híbrido: easter egg → adiós → invocar/fijar → manual → saludos puros → keywords de dominio → **clasificador LLM local** → default. **Blindado a tildes/voseo** (`_sin_tildes`: "traé"="trae", "adiós"="adios"; jun-4). Clasificador: jax_local (qwen2.5:7b), medido **~200 ms**, 4/4 aciertos; fallback a default si falla; migrable vía `set_classifier()`.
- `muscles/base.py` — Contrato `Muscle.invoke(prompt, model=None, history=None)`. HttpMuscle (DeepSeek/Gemini; grounding adjunta fuentes). Excepciones: ModelNotAllowed (fallo duro), Timeout, Invocation.
- `muscles/ollama_muscle.py` — localhost:11434/api/chat, **GPU_SEMAPHORE=1** (una inferencia GPU a la vez).
- `muscles/subprocess_muscle.py` — Hyde: `claude -p --append-system-prompt`, historial serializado a texto (la CLI no acepta array), kill+wait al timeout.
- `voice/kokoro_worker.py` — Proceso dedicado de TTS. Corre con el venv de Kokoro (`~/kokoro-test/.venv/bin/python`), **NUNCA** con el de JAX. `lang_code='e'` (⚠️ 'a' es inglés — error que propuso Deep, corregido). Voces directas (sin mapas). Protocolo: JSON por línea en stdin → 4 bytes LE (tamaño) + WAV por stdout; tamaño 0 = error.
- `voice/tts.py` — VoiceEngine: lazy (worker arranca al primer uso), lock (una locución a la vez), `/callate` mata aplay **sin tomar el lock** (anti-deadlock), limpieza para voz (markdown/código/URLs/emojis fuera), recorte a `MAX_PALABRAS_VOZ` con oraciones completas + "El resto te lo dejo en pantalla". La voz jamás tumba el latido (errores silenciosos). Texto completo SIEMPRE en pantalla.
- `memory/db.py` + worker.py — MariaDB (abajo).

## 5. Memoria (dos sistemas independientes)

1. **Hilo de conversación (corto plazo, RAM):** lista compartida entre TODAS las facetas (un solo JAX), `MAX_TURNS=10` pares, snapshot previo al turno (sin duplicación), historial estructurado role/content insertado en el formato nativo de cada API. Decisión: **compartido** (jun-4).
2. **Persistente (largo plazo, MariaDB 11.8.8):** base `jax_memory`, 9 tablas (conversations, messages, facts, decisions, projects, errors, people, action_items, jax_metadata), VECTOR(768) preparado para embeddings (nomic-embed-text, Fase 2). Usuario `jax_user`@localhost confinado. `messages.conversation_id` es **int**, rol = enum(user/jax_local/jekyll/hyde/hipatia). Escritura fire-and-forget; tolerante a fallos (sin DB, JAX conversa igual). Comandos `/fact list|verify|delete`. Jairo Urbina = primer registro en people (honor_memory=1).

## 6. Voz (Kokoro — Fase 1 COMPLETA, jun-4)

- **Motor:** Kokoro-82M (Apache 2.0, uso comercial libre), español latino, **CPU puro** (torch CPU en `~/kokoro-test/.venv`) — esquiva por completo el muro CUDA/ROCm de la GPU AMD. Calidad validada a oído: **85/100** ("fluido, no perfecto pero funciona").
- **Voces español disponibles:** em_alex, em_santa (masculinas), ef_dora (femenina). Jekyll comparte timbre con Hyde pero a 0.85 (profesor pausado).
- **Medición clave (hall9000):** generación ~4× tiempo real → 650 palabras = 43.3 s de generación para 179 s de audio. El diseño Fase 1 genera TODO antes de sonar → respuestas largas tienen 20-40 s de silencio inicial. `MAX_PALABRAS_VOZ` (en tts.py) controla el techo; decidido subirlo de 90 a ~300 (verificar valor vigente en el archivo).
- **Fase 2 (PRIORIDAD SIGUIENTE): streaming por oraciones** — reproducir la oración 1 mientras se genera la 2; latencia de arranque ~2 s sin importar el largo, techo eliminado. Prompt de diseño para Deep ya redactado; pendiente cruzar su respuesta (⚠️ validar contra evidencia local: Deep ya alucinó lang_code y voice_map).
- Escalera si algún día se quiere premium (pitch del producto financiero): Google TTS → ElevenLabs. Para JAX personal, Kokoro basta.

## 7. Método de trabajo (innegociable)

**POLÍTICAS ADN — instaladas 18-jun-2026 en 3 capas:**
- `~/.claude/CLAUDE.md` — global para Hyde en cualquier proyecto
- `~/jax-platform/CLAUDE.md` — específico de Axioma Platform
- `~/jax/CLAUDE.md` — específico de JAX
- System prompts de Jekyll, Thot y Kimi en `config/config.toml`

Políticas no negociables: **i18n SIEMPRE** (cero strings hardcodeados), **Dark/Light mode SIEMPRE** (CSS variables, nunca colores hardcodeados), **sin hardcoding de ningún tipo**, backup antes de modificar, verificar con evidencia antes de declarar éxito.



- **NO SUPONER.** "El que supone se equivoca; es real hasta que él lo sabe." Si no estás 100% seguro, es un posible error y debe saberse. Método científico: medir antes de decidir (ej.: clasificador ~200 ms medido; voz 43.3 s/650 palabras medido).
- Pasos chicos verificables; mostrar el comando antes de correr; validar en cada fase (wc -l exacto + py_compile).
- Archivos largos: **NUNCA heredoc pegado** (se corrompe en silencio). Claude crea → Fernando descarga → mv/scp. Heredoc solo para scripts cortos throwaway, verificando.
- Backup antes de tocar (`*.backup-pre-<cambio>`).
- Decisiones de arquitectura: cruzar Claude + Deep (+ Hipatia con fuentes). El que tenga evidencia local gana. Los registros/comentarios que mienten se corrigen al detectarse (print "DeepSeek" cuando era local; header del router).
- Fernando decide cuándo termina la sesión; no opinar de horarios.
- Productos sensibles → eventualmente modelos locales (soberanía); desconfianza sana de nubes de terceros.
- **Transparencia del origen (grabado 15-jun):** un dato inventado disfrazado de verificado es peor que un 'no sé' honesto. Cada faceta declara su **origen de autoridad** y NINGUNA respuesta sale sin etiqueta de verificación (🔍 web / 🧠 interno / 📜 solo-input / ✗ abortado / 🏠 local / 🔧 técnico). Hipatia en `required_web` busca de verdad o falla cerrado — jamás inventa fuentes. La voz de la biblioteca, no la del oráculo.

## 8. Pendientes (en orden de intención)

0. **▶ PRÓXIMO HITO — secuenciado por la Mesa, NO diferido (NO-GO operacional de Thot).** Construir el **INTENT ENVELOPE en LAS MANOS**: 16 campos, **rechaza intenciones incompletas**. Nada se conecta a LAS MANOS hasta que el Envelope exista. Después conectar facetas EN ORDEN: **Thot (auditoría) → Hipatia (lectura) → Jekyll (staging) → Hyde (ejecución)**. Principio: *primero quien mira, después quien sabe, después quien construye, al final quien ejecuta.* ⚠️ Procedencia honesta: es decisión VERBAL de la Mesa — aún NO escrita en `six-impossible-things.html` ni en `missions/` (verificado por grep 15-jun); registrada aquí como pendiente vinculante.

1. **Voz Fase 2: streaming por oraciones** (prompt a Deep listo; cruzar, corregir, construir).
2b. **EL OIDO**: COMPLETADO jun-4. Pendiente menor: push-to-talk/wake word — va con la TUI propia (LA CARA terminal) o webapp.
2c. **LA CARA**: webapp ojo de HAL 9000 (disenada 31-may: ojo pulsa al procesar, ondas al hablar, color por faceta; reutiliza Black Diamond Chat + Reverb + React de AteneaERP, ~60% ya existe). Incluye mostrar imagenes y paneles.
2d. **LAS MANOS**: ✅ COMPLETADO (14-jun, ver §9). Sistema de capacidades/ejecución con permisos por faceta + kill switch en vuelo probado. Pendiente menor: extender a más operaciones (http_get, validate_*) y exponerlo a las facetas vía tool calling.
2e. Lanzador `jax` instalado (~/.local/bin/jax) — hecho jun-4.
2. **Chat multiagente / multi-usuario** — diseñar con Deep ANTES de codear. Atado a la decisión de hardware: el cerebro local no escala en una GPU (GPU_SEMAPHORE=1); definir si el producto multi-user usa nube o fierro propio.
3. **Decisión de hardware** (¿devolver RX 9060 XT por NVIDIA 24-32GB?) — sesión propia, con cabeza fresca, NO de madrugada. La GPU solo importa para Ollama local + voz local; 6 de 7 facetas son nube/externas (solo jax_local es local).
4. Hipatia / Gemini: resolver rate limits (tier o espaciado).
5. Easter egg IDE1990 con voz (ahora que Kokoro existe).
6. get_datetime como primer superpoder de JAX local (tool calling Ollama).
3. **LA MEMORIA VIVA** (subida de prioridad jun-4): embeddings + worker de hechos — recuperar de jax_memory lo relevante e inyectarlo al contexto de la faceta. Cura estructural de la confabulacion del 7b (no recordar: leer). Disenar con Deep.
8. Rotación confirmada de la DeepSeek key filtrada en chat (verificar si ya se hizo).
9. Producto financiero (SaaS análisis de crédito): visión central, necesita su propio CONTEXT.md y nombre. La voz premium se justifica ahí, no en JAX personal.

## 9. Historial de hitos

- **Jun 2-3:** Semana 1 — 4 facetas end-to-end, router con clasificador, primer latido, easter egg, base de memoria MariaDB (9 tablas + VECTOR), comandos /fact.
- **Jun 3-4 (esta sesión):** Hilo de conversación compartido (contrato history). Modelo jax_local → qwen2.5:7b (medido). Tono hondureño sobrio. Clasificador migrado a LOCAL (medido ~200 ms, 4/4). **VOZ Fase 1 completa** (Kokoro, 4 voces, /voz, /callate, worker dedicado). Router blindado a tildes/voseo. Registros mentirosos corregidos.
- 2026-06-04 (madrugada): FASE 2 COMPLETA — JAX habla y escucha.
  VOZ streaming v2.1: PCM por oracion + un aplay raw continuo + prefetch real
  (fix underruns: pedir N+1 antes de escribir N). Primera oracion en ~2-3s.
  OIDO: whisper_worker (small int8, 1.5s por 6s de audio) + ears.py + /escucha.
  Mic jack ALC897 calibrado (Capture 70%, boost 0). Defensas: gate RMS 0.01 +
  filtro alucinaciones + initial_prompt con nombres de la casa (fix "John") +
  normalizar_nombres. Boca pronuncia "Yax". Probado con DOS voces: Fernando y
  Claudia (primera invitada). Easter egg con voz. config: "Tu nombre es JAX"
  (fix "Soy Sos JAX") + REGLA DE ORO de la mama en jax_local (derivar
  cuentos/datos; limite documentado: en modo fijo el 7b igual confabula).
  Clasificador local rutea cuentos a Jekyll solo (verificado: 3 cerditos).
  Backups: *.backup-pre-fase2, -pre-nombres, -pre-yax, -pre-egg-voz,
  -pre-regla-oro, -pre-regla-arriba.

- **2026-06-14: LAS MANOS — sistema de capacidades de JAX, COMPLETADO.** Diseño validado por Thot (contención) + Jekyll (arquitectura). API REST local **127.0.0.1:7777** (FastAPI, venv propio `~/jax/las_manos/.venv`). Flujo: **intención → planner → policy engine → human gate → dry-run → worker → audit** (JSONL append-only en `logs/audit.jsonl`). Permisos por faceta: hipatia (solo lectura), jekyll (staging), thot (auditoría), hyde (ejecución completa con human gate en prod). Workers: ssh_worker, file_worker (write atómico `.tmp`+`mv` con snapshot `.bak`), rsync_worker (pull desde destino).
  **DECISIÓN ARQUITECTÓNICA CLAVE: pre-flight gate ≠ in-flight abort.** El kill switch en el portón no frena lo que ya corre. Solución: **watcher async que sondea `/etc/jax/PAUSE` cada 250ms + `ssh -tt`** para propagar SIGHUP al proceso remoto (mata el `sleep` antes del `&& touch`).
  **Cinco garantías probadas en fuego real contra staging (.11):** SSH real (leyó /etc/hostname → atemai-net), policy deny, human gate (sin token deniega / con token permite / token un-solo-uso), dry-run antes de mutar, **kill switch en vuelo** (la prueba brutal: PAUSE a mitad de un `sleep 10 && touch` → el touch NO se creó, verificado con list_dir; audit registró `KILL_SWITCH CRITICAL`).
  **Caveat honesto:** aborta lo que corre, no revierte lo ya escrito a disco. Para eso el snapshot `.bak`. Defensa en profundidad, no magia. ⚠️ Nota de puerto: LAS MANOS usa SSH **58291** para .11/.10 (probado y funcional); hall9000 (.5) usa 58292 (§2). Reliquia del bautismo: `/tmp/las_manos_bautismo.txt` en .11.

- **2026-06-15: FIX GROUNDING de Hipatia — el bug del fallo silencioso, muerto.** Síntesis de tres voces (Thot/Jekyll/Claude). El bug: `google_search` de Gemini es capacidad, no obligación; cuando "cree saber" inventa con confianza sin avisar, y el código entregaba ese invento como válido. **8 decisiones en un solo cambio íntegro:** (1) `grounding_policy` por tarea (off/auto/required_web/local_context_only) reemplaza `grounding: bool`; (2) Hipatia default **required_web**; (3-4) el SISTEMA pone la etiqueta, SIEMPRE, nunca el modelo; (5) valida `chunks` Y `supports`; (6) retry estricto único antes de fallar cerrado; (7) **origen de autoridad para TODAS las facetas** (jekyll 🧠 / thot 📜 / hyde 🔧 / hipatia dinámico / jax_local 🏠); (8) juramento de Hipatia en su system_prompt. Archivos: `muscles/base.py` (grounding_policy + retry + `_append_authority` + `verificacion_label`), `muscles/subprocess_muscle.py` + `muscles/ollama_muscle.py` (param `authority_origin`), `core/main.py` (lee `grounding:` del .md), `config.toml`. **`invoke(decorate=False)`** para usos internos (clasificador del router + extractor de memoria) — la etiqueta no contamina parseos. **Probado en fuego:** prueba determinista (Hipatia intenta inventar noticias → MuscleInvocationError ✗) + 3 casos reales contra Gemini en vivo (requiere-web → 🔍 5 fuentes; noticias de la semana → 🔍 2 fuentes; local_context_only → 📜). Principio grabado (§7 y Six Impossible Things VIII): "un dato inventado disfrazado de verificado es peor que un 'no sé' honesto".

- **2026-06-15 (CIERRE — La Mesa Redonda ratifica la constitución viva).** `~/jax/six-impossible-things.html` pasó de ~98 KB (Apéndices A–B) a **127 KiB / 130 099 B** (md5 `0538c3d9…`; backup inmediato anterior: `six-impossible-things.html.backup-pre-jaxlocal-20260615-030330`, 98 KB / solo A–B). Hitos del día, ratificados por la Mesa (Thot + Jekyll):
  - **Thot es ahora la QUINTA faceta operativa** (ya no consultor externo): motor OpenAI GPT-5.5, provider `openai` en base.py, personalidad `thot` en config.toml. Las facetas operativas son **CINCO**: JAX Local (Qwen3:14b), Jekyll (DeepSeek), Hyde (Claude Code), Hipatia (Gemini), Thot (GPT-5.5). ⚠️ **DERIVA DOCUMENTO-REALIDAD detectada:** §1 y §3 de este mismo documento todavía dicen "4 facetas" — es la amenaza que el Threat Model (Apéndice E) lista como activa; pendiente reescribir §1/§3 con cabeza fresca (el cambio toca la tabla de facetas y el conteo). **[RESUELTO 15-jun: §1 y §3 corregidos a cinco facetas; Thot agregado a la tabla.]**
  - **Tabla canónica de roles** (a pedido de Thot): fija persona/faceta/voz/motor/función para que nadie infiera. Corrige la confusión histórica **Hyde ≠ Hipatia** (Hyde = ejecutor, Claude Code, "tiene las manos"; Hipatia = buscadora, Gemini, "voz de la biblioteca"). Claude (chat estratégico) es **voz** arquitecto, no faceta.
  - **Capítulo XI — Protocolo de la Mesa Redonda:** nadie a la cabecera; la autoridad es del que tiene razón, no del que tiene rango; el corregido convierte el error en guardrail.
  - **Tres meta-contratos nuevos (Apéndices C/D/E), refinados por Thot, NO DIFERIBLES (Principio IX):**
    - **C — Memoria Viva v0.1:** 6 tipos de recuerdo (HECHO/DECISIÓN/PENDIENTE/VERDAD OPERACIONAL/HISTORIA/DESEO·VISIÓN); firma+tipo obligatorios; HECHO y VERDAD OPERACIONAL exigen procedencia; corrección visible, nunca borrado silencioso; **"la memoria informa, no autoriza"**.
    - **D — Enmienda v0.1:** EDITORIAL (corrige hecho/rol/estado; cualquier voz verificando) vs CONSTITUCIONAL (principio/capítulo/contrato; consenso de la Mesa); "el escriba preserva, no gobierna"; **regla de emergencia** (parche que evita daño activo se aplica ya, ratifica después).
    - **E — Threat Model mínimo v0.1:** 10 amenazas activas con fallo cerrado. "El peor caso no es el agente rebelde; es el agente obediente con permisos excesivos y contexto equivocado."
  - **LAS MANOS probado en fuego** (las 5 garantías, ver entrada 14-jun) con permisos por faceta ya incluyendo a thot (auditoría). **Fix de grounding de Hipatia** (ver entrada anterior). **Sésamo operativo** (TrueNAS .6: Restic 2AM + Telegram + ~~Time Machine~~ respaldando Hall9000) ⚠️ aún NO respalda el ecosistema completo (faltan atemai .11 y rich-hn .10). **[CADUCÓ — Time Machine removido el 15-jun por la mañana; ver entrada siguiente.]**

- **2026-06-15 (mañana — EDITORIAL / Memoria Viva: Time Machine removido de Sésamo).** *VERDAD OPERACIONAL caducada → HISTORIA:* se intentó Time Machine en Sésamo; el dataset `timemachine` **no tenía cuota** y devoró **378 GiB del pool de 430**; además un Mac de 500+ GB no cabe en ese pool. Removido el 15-jun por la mañana. *NUEVA VERDAD OPERACIONAL* (procedencia: verificado en la web de TrueNAS, 15-jun): **Sésamo = backup de SERVIDORES.** Restic (hall9000) activo con sus **10 GiB intactos**; pool con **420 GiB libres**. Pendientes: conectar atemai (.11) y rich-hn (.10). Time Machine se mueve a un **disco USB dedicado** (pendiente comprar). El backup de los servidores (lo crítico) nunca dependió de Time Machine — eso era respaldo del Mac de Fernando, que ahora va por separado.

- **2026-06-18 (continuación): Motor Registry v0.2 + systemd + Jacobs bautizado.**
  - **Motor Registry v0.2 ✅:** Kimi real conectada al worker. reasoning_content logueado, invisible al caller. Output validator integrado. Kill switch en vuelo probado. Job lifecycle completo probado en fuego con código real (~94s análisis de worker.py).
  - **LAS MANOS como servicio systemd ✅:** `/etc/systemd/system/jax-las-manos.service`. Arranca con hall9000, reinicio automático en fallo. `curl /health → alive`.
  - **Jacobs bautizado ✅:** Director ejecutivo de pipelines multi-faceta. Nombre en honor al **Prof. Raúl Jacobs** — maestro, mentor, director del colegio donde todo comenzó. Módulo de orquestación puro, sin LLM propio. Frase constitucional (Thot): *"Jacobs dirige el proceso, no decide el propósito. Secuencia, delega y reporta; no inventa autoridad."* Diseño aprobado por Mesa unánime. Hyde construyendo v0.1.

- **2026-06-18 (sesión completa — cierre):**
  - **Jacobs v0.1+v0.2 ✅** — Director ejecutivo de pipelines multi-faceta. En honor al Prof. Raúl Jacobs. Plan builder con JAX Local real. Executor con context propagation (Hipatia→Jekyll→Thot encadenados). Modo autonomous habilitado. Modo supervised con botón APROBAR desde LA CARA. Endpoints: /jacobs/plan, /jacobs/pipeline, /jacobs/pipeline/{id}/resume, /jacobs/pipeline/{id}/approve-step. MariaDB: jacobs_pipelines + jacobs_steps.
  - **JAX Engine ✅** — Estado vivo del ecosistema, EventBus pub/sub, ResourceManager (admission control sin interrumpir workers en vuelo), WebSocket hub por usuario.
  - **LA CARA / Axioma Platform v0.2 ✅** — React 19 + Tailwind + Zustand + react-i18next. Tres paneles: facetas (izq), ojo HAL + chat (centro), Director Jacobs + Audit (der). Modos: Chat (todas las facetas), Comando (Hyde autónomo), Pipeline (Jacobs con modal). Kill switch visible siempre. LAS MANOS vivo en barra inferior. WebSocket con reconexión automática. Tres servicios systemd: jax-las-manos (:7777), jax-platform (:8080), jax-platform-frontend (:5173).
  - **Primer pipeline real de HAMMURABI ✅** — Hipatia (regulaciones CNBS, 10 componentes) → Jekyll (análisis humanista) → Thot (crítica regulatoria). 71KB guardados en ~/jax/workspace/hammurabi-credito-pipeline-001.json.
  - **Políticas ADN instaladas ✅** — CLAUDE.md global + por proyecto + system prompts de Jekyll/Thot/Kimi. i18n obligatorio, dark/light obligatorio, sin hardcoding.
  - **Six Impossible Things actualizado ✅** — Flujograma de dos capas (Infraestructura + Facetas), Ada corregida (GLM-5.2 Z.ai), Kimi en firma final, Jacobs en firma final, portada "Axioma · Infraestructura Cognitiva Personal" en serif dorado.
  - **Rename pendiente:** JAX Platform → **Axioma** (nombre definitivo del producto).

*En memoria de Jairo Urbina. La máquina al servicio de quien construye, no al revés.*

- **2026-06-18: Thot cerrado + Ada bautizada + Mesa Redonda sobre nuevas facetas.**
  - **Thot ✅ CERRADO:** GPT-5.5 operativo vía API OpenAI. Router actualizado (LABELS/ICONS/ALIASES/VALID_FACETAS). Prueba en fuego: "trae a thot" → juicio semántico real activo. Primera auditoría: identifica supuestos ocultos, pide evidencia antes de validar — comportamiento correcto.
  - **Ada bautizada — sexta faceta:** GLM-5.2 (Z.ai/Zhipu), nombrada en honor a Ada Lovelace. Arquitecta de código, 1M tokens de contexto, largo horizonte. Especialidad: leer repos completos, sostener coherencia arquitectónica, coding agéntico multi-hora. Supera a Claude Opus 4.7/4.8 en coding blind test (LMArena #2 global). Key Z.ai pendiente — API general abre semana del 22-jun. Motor ya implementado en base.py (_call_openai compatible); solo falta ZHIPU_API_KEY + entrada en config.toml.
  - **Mesa Redonda — evaluación de Kimi y Grok:** Prompt enviado a Jekyll y Thot. Consenso: Kimi K2.7 Code → **Hefesto** (motor/agente de enjambre subordinado a Ada/Hyde, no faceta constitucional aún). Grok 4.3 → **Cassandra** (radar de señales externas para Hipatia/HAMMURABI, después de Hefesto). Ninguno entra como faceta plena todavía — período probatorio primero. Regla: "No toda inteligencia merece una silla en la Mesa."
  - **Hardware evaluado:** RTX 5090 32GB para hall9000 — desbloquea qwen3:32b completo (~40+ tok/s), CUDA nativo, fin del dolor ROCm. RAM recomendada: 64GB DDR5 (de 32GB actuales). GLM-5.2 local requiere 256GB+ — no entra ni en RTX 5090 ni en RED QUEEN (96GB); siempre vía API.
  - **Router:** Thot agregado a LABELS, ICONS, ALIASES, VALID_FACETAS. Backup: router.py.backup-pre-thot-*.

---
- **2026-06-30 (madrugada — Jacobs "Director de Orquesta" + contrato de capabilities cerrado de raíz). EN PRODUCCIÓN, mergeado a master, pusheado a origin.**
  Sesión de arquitectura+verificación (Fernando + Claude estratégico + Hipatia/Claude Code en hall9000). Método Hyde estricto: cada paso verificado con evidencia, productor≠auditor, producción bajo control de Fernando. El método atrapó CINCO falsos-positivos/estados-rotos: (1) un "test 10/10" que era script con sys.exit(0) y rompía pytest sin correr; (2) servicio sin --reload con código viejo en memoria; (3) catálogo viejo en memoria al intentar e2e prematuro; (4) un commit de Fase A que se saltó por comandos encadenados; (5) una corrección a CONTEXT.md que era no-op (el error vivía en los prompts, no en el doc).
  - **Wave scheduler "Director de Orquesta" (commit 01eacc4) ✅** — run_pipeline reescrito: ejecuta por OLAS topológicas derivadas del DAG (depends_on), steps de una ola en PARALELO vía asyncio.gather. _compute_waves particiona; _run_one_step extrae el cuerpo del step. Estado derivado de refs en context (no cursor lineal) → sobrevive a /resume. Kill switch pre-ola, hyde gate pre-ola, manejo de fallo de ola (respeta skip_on_fail, aborta si falla sin skip), supervised pausa POR OLA (granularidad = ola, no step). _CLEANROOM_RULE en plan.py: auditor debe usar facet distinto al productor.
  - **Contrato de capabilities cerrado de raíz (commit 5fdae32) ✅** — Causa raíz: el planner emitía capability libre, solo kimi la validaba (asimetría), y había nombres sembrados (generate/reconcile/validate_consistency/design/critique/reason) ausentes del catálogo → PIPELINE_ABORTED latente. Fix en 3 capas: (1) config.toml ampliado con 6 capabilities nuevas (generate, reason, design, validate_consistency, reconcile, critique), todas verdes/sin-gate; jacobs añadido a allowed_callers de code_swarm/bug_hunt/architecture_review (gates de code_swarm/bug_hunt INTACTOS, requires_human_gate=true). (2) _CAPABILITY_MAP total (17 entradas, cero pass-through silencioso). (3) VALID_CAPABILITIES en plan.py (planner cerrado, degrada a 'reason' lo desconocido) + validate_capability PRE-dispatch en DOS NIVELES: Nivel A existencia (todos los facets, cierra la asimetría), Nivel B contrato de motor (solo facets-motor/kimi). Fail-open si el catálogo no carga (net secundario, no SPOF).
  - **Suite pytest real (commit edb8099) ✅** — tests/test_jacobs_director.py convertido de script-con-print a 10 funciones test_* con assert. 10/10 vía pytest, integrable a CI. Deuda técnica saldada.
  - **Verificado e2e en producción:** fan-out ada/generate ∥ kimi/generate → thot/validate_consistency. Olas reales [[0,1],[2]], parallel=2 confirmado por timestamps idénticos, orden DAG respetado (ola[0,1] completa antes de ola[2]), kimi/generate ACEPTADO por Motor Registry (202, bug original muerto), clean-room limpio. Pipeline completed.
  - **Servicio/infra:** jax-las-manos.service reiniciado por Fernando, PID vivo, /health 200. Endpoint correcto: POST /jacobs/pipeline (router prefix="/jacobs"). DB jacobs: jacobs_pipelines, jacobs_steps (output_ref LONGTEXT), jacobs_events (columna real event_type, NO type). Timeout reconcile=900s en config (no global). Repo: git@github.com:fjruizhn/Jax.git, master en commit 2c84dd8.
  - **NUEVO DOCUMENTO — AUTONOMIA_ANTIERROR.md:** diseño aprobado de autonomía anti-error en 4 capas. Visión de Fernando: "que el sistema trabaje por mí, no conmigo; auto-auditoría en cada etapa; que no dependa de mí y un yes; lo verdaderamente importante me notifica, lo demás para eso están backups/sandbox." Capa 1 (contrato cerrado) = Fase A, HECHA hoy. Pendientes: Fase B (auto-auditoría bloqueante + schemas), C (snapshot+rollback automático), D (gate de severidad verde/amarillo/rojo + Telegram). Caso de aceptación: ver el Mundial sin mirar el monitor.
  - **⚠️ DERIVA DOCUMENTO-REALIDAD pendiente (Apéndice E, cabeza fresca):** §2 describe hardware viejo (Ryzen 5 8500G / RX 9060 XT) — Fernando arma hall9000 v2.0 (Ryzen 9 9950X / R9700 32GB / 96GB DDR5) hoy. §3 lista Ada como "key Z.ai pendiente" — Ada/GLM-5.2 YA opera en producción vía Z.ai. §8 no menciona Jacobs v0.3 / wave scheduler. Reescribir §2/§3/§8 con cabeza fresca, NO de madrugada.
---

- **2026-08-09: MariaDB 11.8→12.3 LTS (Docker blue/green) + Fase 1 credenciales a la DB. Rama `infra/mariadb-12.3-migration` (jax + jax-platform), aún no mergeada a master.**
  - **Motor: MariaDB 12.3.2 LTS en Docker ✅** — contenedor `mariadb-12-3-jax`, imagen `mariadb:12.3.2` pinneada (nunca `:latest`), puerto `127.0.0.1:3308` (bind explícito a loopback, verificado con `ss`), volumen persistente `/var/lib/mariadb-12.3-docker/data` (bind mount real, no anónimo), red Docker con subred fija `172.30.5.0/24` (grants `@172.30.5.%`, no `@localhost` — el gateway del bridge, no `localhost`, es quien conecta). TZ `America/Tegucigalpa` explícita (el contenedor corría en UTC por default, detectado por un timestamp 6h adelantado). Migración vía dump lógico en 3 fases (schema sin índice VECTOR → carga de datos → `ALTER TABLE ADD VECTOR KEY` en bloque) — evita el riesgo declarado incierto de re-serialización de columnas VECTOR/768d. Paridad verificada: checksums idénticos en 20/20 tablas, `VEC_DISTANCE_COSINE` devuelve resultados idénticos a 11.8. **11.8 nativa apagada pero INSTALADA e intacta — conservar hasta 2026-08-23 (fecha del cutover + 2 semanas) antes de considerar desinstalarla.**
  - **Docker CE instalado desde el repo oficial de Docker Inc** (`download.docker.com`, suite `resolute` = Ubuntu 26.04) — nueva fuente de actualizaciones a vigilar en el ecosistema. `fruiz` NO está en el grupo `docker` (equivale a root) — todo `sudo docker` explícito, vía NOPASSWD acotado en `/etc/sudoers.d/jax-migration` (solo docker + start/stop/restart/status de los 4 servicios JAX — **eliminar esta regla tras el cutover verificado en verde**, `sudo rm /etc/sudoers.d/jax-migration`).
  - **UFW verificado activo el 2026-08-09** (default deny incoming/routed, `:8080`/`:5173` restringidos a LAN) — **R1 de la auditoría (bind 0.0.0.0) queda CERRADO** como riesgo alto; el bind a loopback del contenedor 12.3 es defensa en profundidad adicional, no un parche de firewall roto.
  - **DB como fuente única de verdad para credenciales de proveedor ✅** (resuelve R3: rotar una key no se propagaba sin restart). 3 tablas nuevas (`provider`, `credential`, `credential_audit`), resolver compartido `credential_resolver.py` (espejado en jax-platform/jax/las_manos, mismo patrón que `crypto_secrets.py`) con `resolve_credential()`/`resolve_credential_instrumented()`. `KeyProvider.get_master_key()` como única interfaz para `FERNET_KEY` (`EnvKeyProvider` como única implementación de esta fase). 4+1 consumidores migrados: jax-platform (chat.py/image.py), jax-las-manos/Jacobs (executor.py/plan.py), REPL+jax-memory-worker (vía `jax/muscles/base.py`, resolución por-request, ya no en `__init__`), y Motor Registry/Kimi (`motor_registry/worker.py` — consumidor que se había escapado del inventario original, encontrado durante la implementación). UI admin: rotar y revocar como acciones separadas (antes un solo botón hacía las dos cosas mal), salud persistida en `credential.last_health_status`/`last_verified_at` (antes vivía en estado de React y se perdía al recargar).
  - **TTL = SLA de revocación, explícito:** `CREDENTIAL_CACHE_TTL_SECONDS=30` (una key revocada sigue viva hasta 30s en el caso normal, DB sana) + `CREDENTIAL_STALE_MAX_SECONDS=300` (techo de tolerancia si la DB cae; pasado eso, fail-closed explícito, nunca una llamada silenciosa con credencial vieja). Ambos configurables por env. Verificado en vivo con evidencia real: propagación de rotación en ~15s, fail-closed inmediato tras revocar, degradación explícita tras superar el techo de stale.
  - **Corte con doble lectura instrumentada (B1.4), en curso** — `user_api_keys` (tabla legacy) INTACTA, no se toca hasta que el corte esté verificado. Criterio de salida: 7 días consecutivos sin ninguna lectura `source=env_fallback` en logs, incluyendo al menos una rotación real en la ventana. Si a los 7 días siguen apareciendo, hay un consumidor no mapeado — investigar antes de forzar el corte.
  - **R2 (FERNET_KEY co-ubicada con lo que cifra) — DEUDA ABIERTA, NO resuelta por esta fase.** El TDE de MariaDB protege archivos en disco pero `mariadb-dump` produce SQL en texto plano; el backup a R2 sigue conteniendo la llave y la tabla cifrada en el mismo snapshot restic (confirmado con evidencia: `backup-hall9000.sh` respalda `$STAGING` completo, que incluye tanto `mariadb-local/jax_memory.sql` como `jax-config/env`). `KeyProvider` deja el punto de cambio listo (mover a KMS/Vault el día que se decida es cambiar una implementación, no cazar referencias), pero el tratamiento real queda pendiente como iniciativa propia.
  - **Hallazgo adicional durante la migración:** `/etc/restic/mysql-backup-local.cnf` necesitó `protocol=TCP` explícito además de `host=`/`port=` — el cliente CLI de MariaDB prioriza el socket Unix local sobre host/port si ambos están disponibles; sin ese detalle, el backup hubiera seguido respaldando una base fantasma en silencio.
---

- **2026-08-09: Fase 2 Bloque D — catálogo de modelos y sincronización, diseñado E IMPLEMENTADO (D1+D2). Rama `infra/facetas-bloque-d` (jax-platform; jax solo para este registro), desde `infra/facetas-consolidacion`, aún no mergeada a master.** Diseño completo en `jax-platform/docs/fase2-facetas-diseno.md`. 88/91 tests backend en verde (los 3 restantes son fallas preexistentes de `test_facet_model_wiring.py`, confirmadas no relacionadas con `git stash` antes de empezar).
  - **Catálogo de modelos como entidad propia:** tabla `model` (provider_id, model_id, metadata: contexto/precio/tool_use/modalidades, `status` available/degraded/deprecated/gone, `source` + `source_checked_at` — la procedencia del dato nunca es implícita, mismo principio de "origen de autoridad" del §7). `facet_binding.model_id` (texto libre desde Bloque C) se respalda con `facet_binding.model_ref` (FK real contra `model`) durante una ventana de cutover; la columna vieja se conserva de solo-lectura, no se dropea en esta corrida (mismo criterio que `user_api_keys` en Fase 1).
  - **Alias vs versión fijada — regla permanente:** `model.is_alias` distingue un puntero móvil (ej. `deepseek-chat`, confirmado real contra `models.dev/api.json` con `curl`) de una versión concreta (`deepseek-v4-flash`). Se descartó una columna `alias_of` (a qué apunta el alias hoy): el historial de `resolved_version` + `model_binding_proposal` ya cubre esa pregunta sin mantener una segunda fuente de lo mismo.
  - **`resolved_version` y detección de drift:** `facet_binding.resolved_version` captura lo que el proveedor CONFIRMA haber ejecutado (campo `model`/`modelVersion` de la respuesta real), no lo que se pidió. Capturado fire-and-forget desde la Mesa web (`api/chat.py`, `on_response` opcional en `_call_openai_compat`/`_call_gemini`, no rompe las llamadas existentes) — verificado con test que una falla de captura NO rompe la respuesta al usuario. Si el valor cambia entre dos observaciones, se crea una fila en `model_binding_proposal`: la alerta ES la proposal pendiente, sin tabla de log aparte (decisión explícita, evita una segunda estructura para lo mismo).
  - **`models.dev` — enriquecimiento, nunca verdad de producción para costo.** Verificado contra la API real (`curl https://models.dev/api.json`, 2026-08-09): las claves de proveedor NO coinciden con las nuestras (`gemini`→`google`, `moonshot`→`moonshotai`, `zhipu`→`zai`; solo `openai`/`deepseek` coinciden) — mapeo explícito en `model_catalog.py`, no asumido. La función de enriquecimiento nunca toca `source`/`status`/existencia de una fila, solo metadata (contexto, precio, fechas) — verificado con test.
  - **Regla de oro, mecanismo real (no solo principio):** el sync (`POST /api/admin/models/sync`) solo escribe `model`; un cambio a `facet_binding` pasa SIEMPRE por `model_binding_proposal` + `POST /proposals/{id}/approve` desde un superadmin autenticado (`approved_by`/`approved_at` reales, FK contra `jax_users`) — verificado con test que `reject` nunca toca `facet_binding` y que el sync tampoco, aunque encuentre modelos completamente distintos a los ya bindeados.
  - **Deprecación sin borrado automático (D1.4):** 3 syncs consecutivos sin aparecer un modelo → `degraded`→`deprecated`; nunca `gone` (confirmación manual) ni DELETE. `consecutive_misses` (columna agregada durante la implementación, no estaba en el diseño original) es el contador real.
  - **Bug cerrado con evidencia (el que motivó P5e de la auditoría):** `api/admin/keys.py` mostraba `gpt-4o` hardcodeado para thot cuando la realidad (`facet_binding`, Bloque C) es `gpt-5.5` — pasaba cuando la tabla legacy `facet_models` no tenía fila activa. Fix: `facet_binding`/`model` (fuente real post-Bloque-C) gana sobre `facet_models` sobre el literal. Test reproduce el escenario exacto (desactivando la fila legacy) antes y después del fix.
  - **`axioma_usage` — deuda abierta, anotada, no tocada por este bloque:** `tokens_in`/`tokens_out` siempre en 0, `cost_usd` 0.00 en el 100% de las filas (tabla rota desde antes de Bloque D). El precio de `models.dev` se muestra en el catálogo solo como referencia con su `source` visible — no alimenta ninguna decisión de costo hasta que se capturen tokens reales.
  - **Desviación declarada del diseño original:** D1.6 preveía que el sync de Gemini no necesitara "ningún `if provider_id == 'gemini'` nuevo" — cierto para el transporte de auth (`provider.api_key_transport='query_param'`, genérico), pero **falso** para el parseo de la respuesta: Gemini devuelve `{'models':[{'name':'models/<id>'}]}` en vez de `{'data':[{'id':...}]}`, una asimetría real de forma (no de auth) que sí necesita una rama explícita en `_extract_model_ids` — documentado en el código, no oculto.
  - **Frontend:** 3 pestañas en el panel admin (Proveedores y Credenciales / Catálogo de modelos / Facetas y Bindings), `npm run build` limpio. Contrato de capabilities de facetas (C1.2, antes `unknown` porque el catálogo no existía) ya es un chequeo real. Sin verificación visual en navegador todavía (arrancar el dev server tocaría el puerto del servicio `jax-platform-frontend` — regla dura de no reiniciar servicios sin avisar).
  - **Pendiente explícito para una corrida futura:** wiring de `resolved_version` solo se hizo en la Mesa web (`jax-platform/backend/api/chat.py`); REPL (`jax/core/main.py`) y Jacobs (`jax/jacobs/executor.py`) — los otros 2 de los "3 despachadores" — todavía no capturan `resolved_version` en sus invocaciones reales. La tabla/resolver son compartidos y ya lo soportan; falta solo el wiring en esos 2 archivos del repo `jax`.
---

- **2026-08-09 (continuación): decisión previa al wiring de `resolved_version` en REPL/Jacobs — Hyde y jax_local, con evidencia, ANTES de tocar código.**
  - **Hallazgo real (no supuesto):** REPL (`jax/core/main.py`) NO usa el resolver genérico de Bloque C (`facet_resolver.py`/`ResolvedFacet`) para invocar — eso es exclusivo de Jacobs y de la Mesa web. REPL sigue con su arquitectura de `Muscle` propia (`jax/muscles/base.py` `HttpMuscle`/`jax/muscles/ollama_muscle.py` `OllamaMuscle`/`jax/muscles/subprocess_muscle.py` `SubprocessMuscle`, con URLs por-provider hardcodeadas en `_call_deepseek`/`_call_openai`/`_call_gemini`), solo que desde Bloque C `main.py` sobreescribe `model_default` una vez al arrancar (`load_facet_registry()`) leyendo `facet_binding` en vez de `config.toml`. El wiring de `resolved_version` en REPL toca `jax/muscles/base.py` y `ollama_muscle.py`, NO un resolver compartido — son dos superficies de código genuinamente distintas (REPL vs Jacobs), no una sola.
  - **jax_local (Ollama) — DECISIÓN: se captura, con una limitación documentada.** `ollama_muscle.py` ya hace `data = resp.json()` y descarta todo salvo `message.content` — el campo `model` de la respuesta YA está disponible sin ningún request adicional (verificado leyendo el código, no supuesto). Se captura `resolved_version = data.get("model")` igual que los transportes HTTP. **Limitación real, no ideal:** los tags de Ollama son strings que JAX pide exactos (`qwen3-coder:30b`), no son punteros moviles del lado del proveedor como los alias de nube — Ollama nunca sustituye en silencio. Por lo tanto `resolved_version` va a coincidir con lo pedido casi siempre; un drift real (el mismo tag re-pulleado con pesos distintos) **no es detectable con este campo** — haría falta el `digest` de `/api/show` (otro endpoint, otro request), fuera de alcance de esta corrida. Se captura igual por consistencia de señal entre transportes, con la limitación anotada acá, no vendida como más de lo que es.
  - **Hyde (subprocess, Claude Code CLI) — DECISIÓN: NULL explícito, con motivo, nunca simulado.** `--output-format text` es una decisión firme y documentada desde antes de Bloque D, idéntica en `jax/muscles/subprocess_muscle.py` y `jax/jacobs/executor.py::_invoke_hyde` ("Salida: --output-format text (texto limpio a stdout, parseo cero)"): no hay JSON, no hay campo `model` en la salida, no hay señal de qué versión concreta corrió. **No se llama a `record_resolved_version` para Hyde en absoluto** (ni con NULL forzado a la DB) — el punto de wiring simplemente no existe para este transporte, y esta ausencia queda documentada acá, no silenciada como un descuido. Pasar a `--output-format json` (que sí traería metadata) es una decisión aparte y más grande — toca parseo de stdout, manejo de errores, posible streaming — que no se toma como efecto secundario del wiring de `resolved_version`; queda anotada como opción futura, no ejecutada.
  - **Consecuencia para el catálogo:** las facetas `hyde`/`anthropic` seguirán sin `resolved_version` en `facet_binding` después de este wiring — es la realidad del transporte, no un hueco de implementación. La UI (tab 3, jax-platform) ya maneja esto: sin dato mostrado, no un valor inventado.
  - **Wiring ejecutado ✅** (mismo día, tras la decisión de arriba): `model_catalog.py` espejado en `jax/core` y `las_manos` (solo `record_resolved_version`/`record_resolved_version_safe` — D1.2; el sync de 3 capas y la deprecación NO se duplican, siguen solo en jax-platform). Conectado en `jax/muscles/base.py` (`_call_deepseek`, `_call_openai` — streaming SSE, el campo `model` va en cada chunk — y `_call_gemini` — `modelVersion`, cubre las 4 ramas de `return`), `jax/muscles/ollama_muscle.py`, y en Jacobs (`_invoke_http_gemini`, `_invoke_http_openai_compat`, `_invoke_ollama`). `_invoke_hyde`/`subprocess_muscle.py` llevan un comentario explícito de por qué NO hay wiring (visible en el código, no solo acá).
  - **Detección de drift verificada DISPARANDO end-to-end, no solo el campo capturado** (por instrucción explícita: simular un cambio de `resolved_version` sin tocar el binding y demostrarlo con evidencia). Dos probes manuales contra `jax_memory_test` (nunca tocan producción; HTTP fake-eado, credencial y DB reales): `tests/_probe_resolved_version_drift.py` (Jacobs, `_invoke_http_openai_compat` real) y `tests/_probe_repl_streaming_resolved_version.py` (REPL, `HttpMuscle._call_openai` real, parseo de stream SSE). Ambos: paso 1 (mismo modelo pedido) → sin proposal; paso 2 (proveedor "confirma" un modelo distinto, binding intacto) → `model_binding_proposal` real creada con `reason='drift_detected'`, `status='pending'`, `facet_binding.model_id` sin tocar (regla de oro respetada también en drift). Evidencia impresa (no resumen): proposal id 51 (Jacobs) y 52 (REPL), ambas verificadas y limpiadas del esquema de test después.
  - **Gate de la instrucción (si aparece un 2º `if provider_id == ...` en parseo, parar y avisar): NO se activó.** El wiring usa el mismo patrón de despacho ya existente (una función por transporte — `_call_deepseek`/`_call_openai`/`_call_gemini`, `_invoke_http_gemini`/`_invoke_http_openai_compat`/`_invoke_ollama`), sin agregar ningún condicional nuevo por proveedor dentro de una función compartida. Verificado con `git diff` del commit de wiring: cero ocurrencias nuevas de `provider ==`.
  - **`modelVersion` de Gemini — VERIFICADO contra una llamada real (2026-08-09, `gemini-2.5-flash`, prompt mínimo "di 'ok' y nada más", key enmascarada `AQ.A...bx-g`).** Top-level keys de la respuesta real: `['candidates', 'modelVersion', 'responseId', 'usageMetadata']`. `modelVersion` presente, valor `"gemini-2.5-flash"` — coincide exactamente con el modelo pedido y con el campo que `_call_gemini`/`_invoke_http_gemini` ya leen. Confirma que la captura de Gemini es real, no una suposición.
  - **`model` en cada chunk SSE — VERIFICADO en los 4 proveedores OpenAI-compatible con llamadas reales (2026-08-09, `stream=True`, prompt mínimo "di 'ok' y nada más", keys enmascaradas).** Presente en el primer chunk de los 4: OpenAI (`sk-p...rTUA`), DeepSeek (`sk-4...98a2`), Moonshot (`sk-W...GLuJ`), Zhipu (`86b4...457j`). Confirma que la captura de `_call_openai`/`_invoke_http_openai_compat` es real en los 4 casos, no una suposición.
  - **Hallazgo real durante la verificación — el primer drift genuino que el mecanismo hubiera detectado:** OpenAI resolvió `gpt-5.5` (lo que Jacobs/REPL piden) a `gpt-5.5-2026-04-23` — **`gpt-5.5` es en sí mismo un alias**, no una versión fijada como decía el seed original de `model` (`is_alias=FALSE`). DeepSeek/Moonshot/Zhipu devolvieron exactamente el string pedido — consistente con que esos tres sí son versiones fijadas reales.
  - **Corregido y verificado en producción (`jax_memory`, no `jax_memory_test`) — con aprobación explícita antes de tocar producción.** Precondición real encontrada primero: `jax_memory` **no tenía las tablas de Bloque D todavía** (`jax-platform` corriendo nunca se reinició con esta rama; solo `jax_memory_test`, vía los tests, las tenía). Secuencia ejecutada: (1) backup lógico de `provider`/`facet`/`facet_binding`/`credential`/`facet_models` (`mysqldump`, 13 KB) antes de tocar nada; (2) `run_migrations()` corrido una vez contra `jax_memory` real (mismo código ya probado en test, sin reiniciar el servicio) — verificado con evidencia: `model`/`model_binding_proposal` creadas, 7 filas sembradas, `facet_binding.model_ref` 0 NULL, `facet_binding` sigue en 7 filas (sin duplicar); (3) `UPDATE model SET is_alias=TRUE WHERE provider_id='openai' AND model_id='gpt-5.5'` — verificado, `is_alias=1`; (4) `record_resolved_version("thot", "gpt-5.5-2026-04-23")` real contra producción — `{'drift': False, 'proposal_id': None}`, correcto: es la primera observación en prod (sin valor previo con que comparar), no es un drift a alertar. Verificado: `facet_binding.model_id` sigue en `'gpt-5.5'` (el alias, intacto — regla de oro respetada), `resolved_version='gpt-5.5-2026-04-23'`, cero proposals espurias para thot.
  - **`jax-platform` reiniciado con esta rama (2026-08-09 18:18, a pedido explícito) — EN PRODUCCIÓN, PID nuevo, sin PR mergeado.** El `WorkingDirectory` del servicio (`/home/fruiz/jax-platform/backend`) ya tenía `infra/facetas-bloque-d` checkeada — sin `git checkout` adicional, sólo el restart (`sudo /usr/bin/systemctl restart jax-platform.service`, regla NOPASSWD exacta con `.service`, sin la cual pide contraseña interactiva). Verificado: `NRestarts=0` (sin crash-loop), `/api/health` 200 (`las_manos` también `alive`), `/api/admin/models` y `/api/admin/facet-bindings` responden 401 (no 404 — las rutas nuevas están registradas) sin token. `run_migrations()` re-corrió al arranque, mismos warnings benignos de `INSERT IGNORE` ya vistos, cero errores reales en `journalctl`. **`jax-platform-frontend` (Vite :5173) NO se tocó** — las 3 pestañas nuevas del admin no están visibles todavía, eso es un build+deploy aparte (rsync a la VM dev) que no se pidió en esta corrida.
  - **Frontend build+deploy hecho (mismo día, a pedido explícito) — EN PRODUCCIÓN pública.** Procedimiento exacto de `jax-platform/CLAUDE.md` §Lecciones operativas #1: `npm run build` en hall9000 (293 módulos, limpio) → `rsync` a `/tmp/axioma-deploy/` en la VM dev (172.16.20.11:58291, SSH sin password ya configurado) → `sudo rsync -a --delete --chown=www:www /tmp/axioma-deploy/ /www/wwwroot/axioma-ia.io/` (sudo en la VM dev es `NOPASSWD: ALL` para `fruiz`, verificado con `sudo -n -l` antes de usarlo). Verificado con evidencia real: `https://axioma-ia.io/` → 200, referencia `assets/index-CEDfogAs.js` (mismo hash del build fresco), ese asset → 200. Las 3 pestañas de Bloque D (Proveedores y Credenciales / Catálogo de modelos / Facetas y Bindings) están en producción pública desde acá.
---

- **2026-08-10: BUG REAL encontrado probando en el navegador — `facet_models` (legacy) seguía viva y desincronizada de `facet_binding`, editable desde la pestaña 1. Diagnosticado y cerrado, misma sesión, mismo día que el deploy de arriba.**
  - **Síntoma reportado por Fernando, verificado con evidencia antes de tocar nada:** la pestaña "Proveedores y Credenciales" mostraba `gpt-5.6-terra` (thot) y `gemini-3.5-flash` (hipatia); las pestañas "Catálogo de modelos"/"Facetas y Bindings" (Bloque D, `facet_binding` real) mostraban `gpt-5.5` y `gemini-2.5-flash`. Confirmado en `jax_memory` real: `facet_binding` tiene esos dos últimos valores; `facet_models.is_active=1` tiene los dos primeros. Dos pestañas de la misma app, mismo momento, afirmando cosas distintas del mismo hecho.
  - **Diagnóstico completo, no cosmético:** el dropdown de la pestaña 1 (`api/admin/facet_models.py`) lee/escribe EXCLUSIVAMENTE `facet_models`. Pero esa tabla está **muerta para el comportamiento real** desde Bloque C: `chat.py::_resolve_active_model` y `jacobs/executor.py::_get_active_model` — las dos únicas funciones que la leían para decidir qué modelo invocar — estaban definidas pero **sin ningún caller** (verificado con `grep`, no supuesto). `resolve_facet()`/`facet_binding` las reemplazó y nunca se borraron. El dropdown de la pestaña 1 no mostraba un dato viejo: era un control **inerte** — cambiarlo no cambiaba qué modelo corre. Peor que "cuál miente": una de las dos superficies ya no controlaba nada, pero se veía y se sentía exactamente igual de real que la que sí.
  - **Causa raíz de por qué pasó:** Bloque D reorganizó la pestaña 1 en 3 pestañas pero **conservó el contenido original de la pestaña 1 sin verificar si seguía conectado a algo real** — "no romper lo que funciona" sin comprobar que efectivamente funcionaba (que efectivamente hacía algo). Bloque C había cortado la invocación real semanas/sesiones antes; nadie volvió a mirar si el panel admin lo reflejaba.
  - **Fix (cierra el camino de doble escritura, no lo parchea):**
    - `AdminApiKeys.jsx` (jax-platform): se borra el dropdown FACETA/MODELO y toda su UI de gestión (columnas, popover, agregar/borrar, modal de confirmación aritmética). La pestaña 1 queda pura Proveedores y Credenciales — faceta→modelo es dominio exclusivo de la pestaña 3 desde ahora, sin mezcla de entidades.
    - `chat.py::_resolve_active_model` y `jacobs/executor.py::_get_active_model` — borrados (dead code confirmado).
    - `api/admin/facet_models.py` (el router) — **desregistrado** de `main.py`/`api/admin/__init__.py`. Sin ninguna UI que lo llame, seguía siendo un endpoint vivo y alcanzable que podía volver a escribir `facet_models` sin que nada lo notara. La tabla `facet_models` y el módulo Python **se conservan** (dato histórico) — el borrado completo queda diferido, decisión explícita no tomada en esta corrida.
  - **Efecto colateral bueno, no buscado:** `tests/test_facet_model_wiring.py` tenía 3/5 tests en rojo desde ANTES de que arrancara Bloque D (confirmado con `git stash` al principio de esa corrida) — nunca fueron "no relacionados", eran tests que seguían mutando `facet_models` mientras el código real ya leía `facet_binding`. Reescritos contra `facet_binding` (la fuente real). **90/90 tests en verde** — los 3 rojos que arrastramos toda la sesión quedan genuinamente resueltos, no solo descartados.
  - **Redeploy:** `jax-platform` reiniciado de nuevo con el fix (mismo procedimiento que el restart anterior — verificado sin errores, `/api/admin/facet-models/*` ahora 404 real), frontend rebuildeado y redesplegado a `axioma-ia.io` (mismo procedimiento de `jax-platform/CLAUDE.md`; hash del bundle nuevo confirmado servido por curl).
  - **Hallazgo adicional durante la verificación en navegador — caché del lado del cliente, no del servidor.** El vhost de `axioma-ia.io` no manda `Cache-Control` explícito en `index.html` (solo `ETag`/`Last-Modified`) — Chrome, con caché heurística, siguió sirviendo el HTML/JS VIEJO en pestañas nuevas (no solo la que ya tenía la página abierta) hasta forzar bypass de caché (`?cachebust=`). Confirmado con evidencia: la misma URL sin cachebust seguía disparando `GET /api/admin/facet-models/*` (404, código viejo) en una pestaña recién abierta; con cachebust, la misma URL cargó el bundle nuevo y esas llamadas desaparecieron. El deploy en sí es correcto (confirmado por curl con hash de archivo) — el riesgo real es que Fernando (o cualquier visitante) puede seguir viendo la versión vieja en su propio Chrome hasta un hard-refresh (Ctrl/Cmd+Shift+R) o hasta que la caché heurística expire por su cuenta. **Corregido, mismo día, a pedido explícito.** Backup del vhost (`axioma-ia.io.conf.backup-pre-nocache-20260810`, 55 líneas, vía `sudo cp`) antes de tocar nada. Agregado `location = /index.html { add_header Cache-Control "no-cache"; }` — `try_files ... /index.html` hace un internal redirect que pasa por ese bloque para toda ruta SPA, no solo `/`. `/assets/` conserva su cache 1y immutable (sin tocar, ya es correcto: nombre de archivo con hash de contenido). Validado con `sudo nginx -t` ANTES de `sudo nginx -s reload` (reload, no restart — no corta conexiones). Verificado con evidencia real: `curl -I https://axioma-ia.io/` → `Cache-Control: no-cache`; `curl -I .../assets/...` → sigue en `max-age=31536000, immutable`; sitio sigue en 200. **Caveat real, no ocultado:** el header nuevo solo cambia el cacheo de respuestas FUTURAS — no invalida lo que Chrome ya tenía guardado de antes del reload (confirmado: una pestaña nueva sin cachebust siguió mostrando la UI vieja hasta un `Ctrl+Shift+R` explícito). Ese hard-refresh sí mostró la UI correcta. A partir de este deploy, los próximos no deberían repetir la confusión — la caché vieja que ya existe en navegadores de otros visitantes se resuelve sola cuando expire o con su propio hard-refresh, no hay forma de invalidarla desde el servidor retroactivamente.
  - **PR sigue en draft** (instrucción explícita: no sacarlo hasta resolver esto).
  - **Último cambio antes de sacar el PR de draft: rename "API Keys" → "Facetas & Modelos" en el menú admin (a pedido explícito).** Ruta real corregida antes de tocar nada: `/admin/keys` (no `/admin/api-keys` como se supuso al pedirlo) — **se conserva sin cambios**, decisión explícita para no romper bookmarks/enlaces guardados. i18n `adminApiKeys`→`adminFacetsModels` (mismo key alimenta sidebar + `<h1>`), icono 🔑→🧠, componente `AdminApiKeys.jsx`→`AdminFacetsModels.jsx` (`git mv`, preserva historia). `AdminDashboard.jsx` "API Keys" (stat card real de credenciales configuradas) **sin tocar** — no es el nombre de la sección. Build limpio, 90/90 tests, desplegado y verificado en el navegador **sin necesitar hard-refresh** (el fix de `Cache-Control` de la entrada anterior ya funcionando solo, primera prueba real de que sirvió).
---

- **2026-08-10: sync automático del catálogo de Anthropic — investigado, decidido e implementado (TDD), mismo día. Rama `infra/facetas-bloque-d` (jax-platform). PR sigue en draft.**
  - **Corrección de una afirmación previa mía, con evidencia real:** había dicho que Anthropic no ofrecía listado programático de modelos y propuesto curación manual. Fernando lo cuestionó ("pero me gustaria una forma automatica"). Cargué la skill `claude-api` y verifiqué contra la API real (`curl`, 2026-08-10): `GET https://api.anthropic.com/v1/models` responde `200` usando como Bearer el token OAuth que el propio `claude` CLI (Hyde) ya deja en `~/.claude/.credentials.json` (`sk-ant-oat01...`, scope `user:inference`) — NO hace falta `ANTHROPIC_API_KEY`. Catálogo real devuelto: `claude-opus-5`, `claude-sonnet-5`, `claude-fable-5`, `claude-opus-4-8/4-7/4-6`, `claude-sonnet-4-6`, `claude-opus-4-5-20251101`, `claude-haiku-4-5-20251001`, `claude-sonnet-4-5-20250929`. Confirma que Fable ya está disponible para bindear a Hyde.
  - **Vigencia del token — verificada, no supuesta:** `expiresAt` de vida corta (horas), `refreshTokenExpiresAt` semanas. `claude` CLI se refresca solo cada vez que Hyde corre, así que el archivo se mantiene fresco en uso normal.
  - **Decisión de diseño (discutida con Fernando, no autoimpuesta): NO se implementa refresh OAuth propio.** Fernando propuso inicialmente ir por un refresh propio ("independiente"); el argumento que cambió su decisión: un refresh en OAuth típicamente ROTA el refresh token — un bug ahí podría invalidar el refresh token real de Hyde y forzar un re-login manual de la sesión de producción, por un beneficio menor (evitar que el sync espere unas horas a que Hyde se use). **Opción elegida: leer el archivo en caliente en cada sync; si el token está vencido o el archivo no existe, el sync se salta con motivo explícito en el resultado (`skipped`) y reintenta en la próxima corrida — nunca escribe, nunca reintenta con credencial vieja.**
  - **Hallazgo de política que refuerza la decisión — buscado, no supuesto (`WebSearch`, 2026-08-10):** Anthropic prohibió explícitamente desde feb-2026 el uso de OAuth de suscripción (Free/Pro/Max) en apps de terceros — "OAuth from Free, Pro, and Max plans is intended exclusively for Claude Code and Claude.ai"; no dan de alta client IDs nuevos para terceros; ya hay reportes de aplicación técnica, no solo de política escrita ([winbuzzer.com](https://winbuzzer.com/2026/02/19/anthropic-bans-claude-subscription-oauth-in-third-party-apps-xcxwbn/), [github.com/anthropics/claude-code#28091](https://github.com/anthropics/claude-code/issues/28091)). El sync de hoy vive en zona gris tolerable (lee un token de sesión local propia, no ofrece login de terceros a nadie) pero **no es un integration point soportado** — se trata como best-effort, sin inversión adicional de robustez, consistente con la decisión de arriba.
  - **Implicación para multi-usuario ("usuarios conjuntos") — respondida con evidencia, no placeholder para después:** el patrón de hoy (leer `~/.claude/.credentials.json`) es inherentemente single-tenant, ligado a un login de `claude` CLI por máquina/usuario de OS. **(a) Dar Axioma a otra persona en su propio deployment:** sigue siendo simple — esa persona corre su propio `claude login` en su propia máquina, su instancia lee su propio archivo, cero código nuevo. **(b) Multi-usuario real en un solo deployment de Axioma:** el patrón OAuth-de-suscripción-de-terceros está explícitamente prohibido por Anthropic (ver hallazgo de arriba) — no es viable ofrecer "Login with Claude" a un segundo usuario y capturar su token. El único camino sancionado para ese escenario es el que YA usan los otros 5 providers: `ANTHROPIC_API_KEY` real por usuario (Console, de pago), vía el mismo sistema de credenciales (`AdminFacetsModels.jsx`, rotate/revoke). **Decisión registrada para cuando llegue esa corrida: NO extender el patrón OAuth a multi-usuario, diseñar esa pieza desde cero con API key por usuario.**
  - **Implementado (TDD, RED confirmado antes de escribir código):**
    - `model_catalog.py`: `_read_anthropic_oauth_token()` (lee `~/.claude/.credentials.json`, valida `expiresAt` contra `time.time()`, lanza `AnthropicOAuthUnavailableError` si falta/vencido) + rama `if provider_id == "anthropic"` en `sync_provider_models` (credencial local en vez de `resolve_credential_instrumented`, header `anthropic-version: 2023-06-01` adicional al `Authorization: Bearer`, catch de `AnthropicOAuthUnavailableError` → resultado `skipped` explícito, nunca una excepción que tumbe el resto del sync). El parseo de la respuesta (`{'data':[{'id':...}]}`) no necesitó rama nueva — ya coincidía con el shape genérico OpenAI-compatible que `_extract_model_ids` maneja por default.
    - `db/migrations.py`: `anthropic` sumado a `_PROVIDER_SYNC_SEED` (`header_bearer`, `https://api.anthropic.com/v1/models` — URL real verificada con curl, no inventada); `_fix_anthropic_sonnet_alias()` nueva, corrige `is_alias` de `anthropic/sonnet` de `FALSE`→`TRUE` (quedó mal seteado en el seed original de Bloque C0 — "sonnet" es un alias de tier, no una versión fijada, confirmado contra el catálogo real de arriba, mismo patrón que el bug de `openai/gpt-5.5` cerrado el 2026-08-09).
    - `api/admin/models.py`: `anthropic` sumado a `_SYNCABLE_PROVIDERS`.
    - Tests nuevos en `test_model_catalog_sync.py` (token OAuth válido → upsert correcto + header verificado; archivo ausente → skip sin request HTTP; token vencido → skip sin request HTTP) + fix de una aserción hardcodeada en `test_admin_models_endpoints.py` que no incluía `anthropic` en la lista esperada de providers sincronizados. **93/93 tests backend en verde.**
  - **Desplegado en producción (`jax_memory` real), con aprobación explícita antes del restart.** `sudo systemctl restart jax-platform.service` — arriba sin crash-loop (`NRestarts=0`), `/api/health` 200. Verificado con evidencia contra la DB real: `provider.anthropic.models_list_url`/`api_key_transport` sembrados, `model.anthropic.sonnet.is_alias=1`. Sync real disparado vía `POST /api/admin/models/sync` con JWT superadmin real: **`anthropic` trajo 10 modelos reales** (`claude-fable-5`, `claude-opus-5`, etc., `source='provider_api'`), `facet_binding` de hyde intacto (`model_id='sonnet'` sin tocar — regla de oro confirmada en producción, no solo en test). Hallazgos no relacionados durante la verificación, anotados y NO tocados (fuera de scope): `openai` falló ese sync puntual con `ReadTimeout` (transitorio), `enrich_from_models_dev` falla con `release_date` en formato `'2026-01'` (bug preexistente de otro provider, no de anthropic).
  - **Bug real encontrado en la propia verificación, corregido el mismo día (TDD de nuevo, no parche directo en prod):** el primer sync real degradó `anthropic/sonnet` a `status='degraded'` — Anthropic `/v1/models` jamás lista un alias de tier suelto (`sonnet`), solo los IDs fechados detrás del alias, así que D1.4 (3 misses → deprecated) lo iba a marcar como si estuviera desapareciendo, aunque Hyde lo sigue invocando sin problema. Test rojo primero (`test_sync_provider_models_anthropic_never_deprecates_bare_alias`, reprodujo el bug con evidencia antes de tocar código). Fix en `model_catalog.py`: en el conteo de misses, `provider_id=="anthropic"` + `model_id` sin prefijo `claude-` → se salta, ausencia estructural, no una señal real. Reparación del dato ya corrompido en prod plegada a `_fix_anthropic_sonnet_alias` (migrations.py, guard `status='degraded'`, no pisa un `deprecated`/`gone` real de otra causa) — no un `UPDATE` manual suelto, queda versionado y reproducible. **94/94 tests, redeploy, verificado con un segundo sync real: `sonnet` se mantiene `available`/`consecutive_misses=0`.**
---

- **2026-08-10 (continuación, mismo día): sync automático del catálogo de Ollama local — implementado (TDD), sobre el mismo `sync_provider_models`. Rama `infra/facetas-bloque-d`.**
  - **Shape real verificado con curl antes de escribir código (2026-08-10):** `GET http://localhost:11434/api/tags` (sin auth, `provider.auth_type='none'`) responde `{'models':[{'model':<tag>, 'digest':<sha>, 'size':..., 'modified_at':..., 'details':{...}}]}` — tercera forma de respuesta distinta a las ya vistas (ni `{'data':[{'id':...}]}` de los OpenAI-compatible/Anthropic, ni `{'models':[{'name':'models/<id>'}]}` de Gemini), rama explícita nueva.
  - **`digest` capturado — cierra una limitación documentada el 2026-08-09.** Un tag de Ollama (`qwen3-coder:30b`) es un puntero LOCAL, no un alias del lado del proveedor: puede re-pullearse con pesos distintos sin que el tag cambie — la entrada de esa fecha en este mismo documento decía explícitamente que ese drift "no es detectable con este campo" (el `model` de la respuesta de invocación) y "haría falta el `digest` de `/api/show` ... fuera de alcance de esa corrida". Hoy sí se captura: columnas nuevas `model.digest`/`model.digest_changed_at`. `digest_changed_at` queda `NULL` en la primera observación (sin "antes" con que comparar) y se pobla solo cuando el digest cambia de verdad entre dos syncs — logueado como warning (`model_catalog ollama digest changed model=... from=... to=...`), **sin** generar `model_binding_proposal`: el tag sigue siendo el mismo string, no hay un `model_ref` nuevo al que proponer cambiar — ese mecanismo está diseñado para "cambiar a qué apunta el binding", no para "el mismo puntero ahora significa otra cosa". Decisión explícita: exponer el dato (columnas + log), no inventar un flujo de aprobación para una alerta de forma distinta — queda abierto si Fernando quiere una superficie más visible (ej. en la pestaña de catálogo) en una corrida futura.
  - **Sin credencial en absoluto** (a diferencia de anthropic, que sí resuelve un token OAuth): `provider.auth_type='none'`, la rama `if provider_id == "ollama"` en `sync_provider_models` bypassa `resolve_credential_instrumented`/headers por completo — verificado con test que la request no lleva `Authorization` ni ningún otro header.
  - **Fail-soft ante Ollama caído o el semáforo de GPU ocupado:** cualquier excepción en el `GET` (conexión rechazada, timeout) se atrapa y el sync de ese provider se salta con motivo explícito (`skipped`), nunca tumba el resto del `/sync` (openai/anthropic/etc. siguen su curso) — mismo patrón fail-soft que anthropic, mismo motivo: nunca una excepción sin manejar interrumpiendo un loop que sirve a 6 providers.
  - **`_PROVIDER_SYNC_SEED` sumó `ollama` (URL real `http://localhost:11434/api/tags`, transport queda en el default inerte porque el código nunca lo lee para este provider).** `_SYNCABLE_PROVIDERS` (api/admin/models.py) sumó `ollama`.
  - **TDD igual que anthropic (RED confirmado antes de cada implementación):** token/auth ausente verificado, digest capturado y `digest_changed_at` verificado en dos syncs consecutivos con distinto digest, unreachable→skip sin excepción. **Hallazgo de proceso durante la propia verificación:** el test de digest falló en la corrida de la suite completa (pasaba solo con `-k ollama`) — no un bug de la implementación, sino contaminación real entre corridas: `jax_memory_test` es persistente (documentado en el propio docstring del archivo desde antes), así que una corrida manual previa de `-k ollama` dejó un digest distinto guardado, y el test asumía sin querer una baseline limpia. Corregido con el mismo patrón que `test_sync_marks_missing_model_deprecated_after_three_consecutive_misses` ya usaba (reset explícito de baseline antes de la aserción, no asumir estado inicial). **97/97 tests backend en verde, confirmado estable en 2 corridas completas seguidas de la suite (no solo una).**
  - **Desplegado en producción**, mismo procedimiento que el bloque anterior (restart + verificación con evidencia real contra `jax_memory` y un sync real).
  - **Commiteado** (`5aa8286`, `infra/facetas-bloque-d`) — el trabajo de anthropic+ollama vivía sin commitear en el working tree del servicio en vivo hasta acá; cerrado a pedido explícito.
  - **PR #3 (`infra/facetas-bloque-d` → `infra/facetas-consolidacion`) sacado de draft a pedido explícito de Fernando (2026-08-10).** Criterio dado por él para el "ready": diseño + implementación + 90/90→97/97 tests + el bug real de `facet_models` encontrado y corregido + verificación en navegador + fix de nginx — ya cumplido, sin nada nuevo que agregar. El rename del menú (`2654b30`) ya estaba en la rama, no hizo falta secuenciarlo.
  - **Sacar de draft ≠ mergear — distinción explícita de Fernando, no tocada hoy.** Cadena de bases real: `infra/mariadb-12.3-migration` (jax + jax-platform) → `infra/facetas-consolidacion` → `infra/facetas-bloque-d`. Mergear el de arriba sin los de abajo no tiene sentido. **Disparador: criterio de salida de B1.4 (doble lectura credenciales DB→env, ver entrada 2026-08-09 "MariaDB 11.8→12.3") vence el 2026-08-16** — 7 días consecutivos sin ninguna línea `source=env_fallback` en logs, incluyendo al menos una rotación real en la ventana. Si al 16 el grep sale vacío: se retira el fallback a `.env` y ahí se mergea la cadena completa, en este orden: (1) `infra/mariadb-12.3-migration` → master, (2) `infra/facetas-consolidacion` → master, (3) `infra/facetas-bloque-d` → master. Si el grep NO sale vacío al 16, hay un consumidor de credenciales no mapeado — investigar antes de forzar el corte (ya anotado el 2026-08-09).
---

- **2026-08-10 (continuación): bug real en Kimi/Motor Registry — diagnosticado y corregido con evidencia, no con las hipótesis originales. Repo `jax`, rama `infra/facetas-bloque-d` (verificado con `git branch`, no supuesto — la primera versión de esta entrada decía "master" sin chequear, corregido acá mismo).**
  - **Punto de partida:** Fernando reportó "creo que falló kimi" en un pipeline. Primer hallazgo, contra la evidencia: no había ningún pipeline creado *hoy* en `jacobs_pipelines` — el más reciente era de **ayer 2026-08-09 04:45** ("Analiza las mejores prácticas... Jarvis"), con los 5 steps `status='completed'`, `error=NULL`, incluido kimi. Fernando corrigió con la hora exacta (04:46:25) y "hay archivos generados en el repo" — eso llevó a los documentos reales del pipeline en `jax/repo/documents/`.
  - **Evidencia del bug real:** `81c09f7f_03_kimi.md` (el documento del step de kimi) pesa **488 bytes**, contra 20-25KB de los otros 4 facets del mismo pipeline, y termina literalmente a mitad de palabra ("...hasta mi fecha de co"). Confirmado que no es un artefacto de visualización: `LENGTH(output_ref)` en `jacobs_steps` da **861 bytes reales** en la DB — el corte es genuino, no de renderizado.
  - **Las tres hipótesis originales (credencial / transporte / modelo), descartadas con pruebas en vivo, no por descarte teórico:** llamada real a `https://api.moonshot.ai/v1/chat/completions` con la credencial activa real (desencriptada de la tabla `credential`) y el modelo real (`kimi-k2.7-code`) — responde `200`, `finish_reason: stop`, contenido completo. Las tres hipótesis quedan descartadas con evidencia, no supuestas como buenas.
  - **Causa real encontrada leyendo `motor_registry/worker.py`:** `_call_kimi` nunca mandaba `max_tokens` en el payload. `kimi-k2.7-code` es un modelo de razonamiento (`supports_reasoning=true`) — el campo `reasoning_content` cuenta contra el mismo budget de completion que `content`. Reproducido en vivo: un prompt trivial ya gastó 467 de 866 completion_tokens en razonamiento (más de la mitad) sin ningún `max_tokens` puesto — con el prompt real y demandante de ayer (mucho más largo, con instrucciones de "regla de evidencia" + una consulta técnica de 6 puntos), es plausible que el razonamiento se comiera el budget completo del default de Moonshot antes de escribir la respuesta final.
  - **Confirmación definitiva — el mismo prompt exacto que falló ayer, repetido en vivo con `max_tokens=8000`:** `finish_reason: stop`, `completion_tokens=6579` (de 8000 disponibles, sin tocar el techo), `content` completo de 15325 caracteres terminando en una oración natural. Sin el fix, el mismo prompt se cortaba; con el fix, no. No es una corrección teórica — está verificada contra el caso real que reportó Fernando.
  - **Segundo problema encontrado en el camino, igual de real:** `worker.py` extraía `content`/`reasoning_content` de la respuesta y descartaba todo lo demás — `finish_reason` y `usage` nunca se guardaban. Sin eso, diagnosticar ESTE mismo incidente hubiera sido imposible sin la reconstrucción manual que se hizo hoy (decrypt de la key, curl directo, comparar tamaños de archivo). Cerrado también.
  - **Implementado (TDD — sin infraestructura de pytest en este repo, se usó `unittest.IsolatedAsyncioTestCase` de la stdlib para no sumar una dependencia nueva por un fix puntual):**
    - `motor_registry/catalog.py`: `MotorEntry.max_tokens: int = 0` (0 = no mandar el campo, compat con motores sin esto en config).
    - `motor_registry/worker.py`: `_call_kimi` manda `max_tokens` en el payload cuando el motor lo declara; se capturan `finish_reason`/`usage` de la respuesta real y se guardan como `_finish_reason`/`_usage` en el job (mismo patrón `_`-prefijo no expuesto que ya usaba `_reasoning_content`); `logger.warning` explícito cuando `finish_reason == 'length'` — visible en `journalctl` en el momento, no solo reconstruible después.
    - `config.toml`: `max_tokens = 8000` en `[motors.kimi]` y `[motors.ada]` (ambos `supports_reasoning=true`, mismo riesgo) — valor de partida generoso sobre lo observado en las pruebas reales, no un techo medido contra el límite real de Moonshot/Z.ai; ajustar si se repite un corte.
    - Test nuevo `_worker_max_tokens_test.py` (3 casos: `max_tokens` viaja en el payload, `_finish_reason`/`_usage` se guardan, el caso real `finish_reason='length'` queda registrado) — mockea solo el límite de red real (`httpx.AsyncClient.post`) y la credencial; `JobStore`/`MotorCatalog` corren de verdad contra un JSONL temporal. RED confirmado (los 3 fallaban por la razón correcta) antes de escribir el fix.
  - **Desplegado**, `jax-las-manos.service` reiniciado para tomar `config.toml`/código nuevo, verificado sano.
  - **Hallazgo adicional, sin tocar:** en los logs de `jax-las-manos` de hoy aparece un patrón repetido de `GET /jacobs/pipeline` (405, el endpoint solo acepta POST) seguido de `GET /jacobs/pipeline/<uuid-random>` y `.../results` (404), con UUIDs distintos cada 30-90 min durante todo el día — no es un pipeline real (nunca hay un `POST /jacobs/pipeline` hoy), parece algún cliente/script externo consultando mal el endpoint. No investigado más, anotado para revisar si vuelve a aparecer.
---

- **2026-08-10 (continuación): Costos 100% funcional — ejecutado con Subagent-Driven Development (superpowers), spec + plan + 7 tasks + fix waves + deploy. Repos jax-platform y jax.**
  - **Investigación previa (brainstorming):** `axioma_usage` tenía `tokens_in`/`tokens_out` en 0 y `cost_usd` 0.00 en el 100% de las filas, deuda anotada desde antes de Bloque D. Causa real: `record_usage(...)` en `chat.py:577` se llamaba con `0, 0` **literales**, no datos faltantes. Se encontraron 6 problemas distintos: tokens hardcodeados, `model_name` leído de `config.toml` (stale) en vez del binding real, `image.py` nunca llamaba `record_usage`, Hyde sin señal de tokens (`--output-format text`, límite estructural ya conocido), `MODEL_PRICES` un dict hardcodeado duplicado de la tabla `model`, y **Kimi no pasa por `chat.py` en absoluto** (`transport='motor_registry'`, solo invocable vía Jacobs) — y los pipelines de Jacobs no llevaban identidad real de usuario (`api/pipelines.py` reenviaba `"invoked_by": "Fernando"` hardcodeado).
  - **Spec** (`jax-platform/docs/superpowers/specs/2026-08-10-costos-100-funcional-design.md`) y **plan** (`jax-platform/docs/superpowers/plans/2026-08-10-costos-100-funcional.md`) escritos y aprobados antes de tocar código — decisiones explícitas: Hyde queda sin tracking (documentado, no simulado), precios vienen de la tabla `model` (Bloque D) no de un dict aparte, Kimi entra al alcance incluyendo arreglar la identidad de pipelines como prerequisito (Fernando eligió el alcance mayor sobre la opción de un tenant fijo de sistema).
  - **7 tasks ejecutadas vía subagent-driven-development** (implementador + review de spec/calidad + fix loop por task, ledger en `.superpowers/sdd/2026-08-10-costos-100-funcional/`): Parte A (jax-platform) — `record_usage()` calcula costo real desde `model` (Task 1, encontró y corrigió una desviación propia: un implementer cambió `user_id`/`tenant_id` de `str` a `int` en la firma, revertido); `chat.py` captura tokens reales de los 3 transportes directos (Task 2); `image.py` registra costo plano por imagen vía `cost_usd_override` (Task 3, 2 rondas de fix por un archivo de test duplicado sin commitear el delete). Parte B (jax-platform + jax) — identidad real inyectada en `api/pipelines.py` (Task 4); `Pipeline`/`PipelineCreateRequest` cargan `user_id`/`tenant_id` real (Task 5, jax); `executor.py` propaga la identidad al dispatch de Kimi (Task 6, jax); `las_manos/motor_registry` escribe `axioma_usage` real cuando hay identidad (Task 7, jax, `usage_writer.py` nuevo). Cada task: TDD (RED confirmado antes de implementar), review de spec+calidad, commit.
  - **Revisión final de todo el branch (2 reviewers en paralelo, un repo cada uno, modelo más capaz) encontró hallazgos reales que ningún task review individual pudo ver — exactamente el motivo de tener una revisión final aparte:**
    - **jax-platform, Critical:** (C1) `AdminCosts.jsx` crasheaba con `cost_usd: null` (`.toFixed(6)` sin guardar) — verificado contra el catálogo real: **180 de 218 filas de `model` no tienen precio**, así que el primer chat con thot/ada/jax_local/kimi tras el deploy rompía la pestaña que esta corrida existe para arreglar. Ninguna task tocó el frontend, así que ningún task review lo vio. (C2) el test de imágenes de Task 3 era un falso verde — `int(tenant_id)` con un fixture no-numérico (`"test-image-pooling-tenant"`) fallaba silenciosamente (atrapado por el `except` best-effort), y el test leía una fila que había dejado OTRO archivo de test en una corrida anterior — mismo patrón de contaminación entre corridas de `jax_memory_test` ya visto una vez hoy con el test de digest de Ollama.
    - **jax-platform, Important (5):** excepciones silenciosas sin loguear, `SUM(cost_usd)` puede mostrar un total incompleto sin avisar (NULLs se saltan en silencio), `chat.py` guardaba dos "modelos" distintos para el mismo turno (memoria vs usage — mismo patrón de staleness que el bug gpt-4o/gpt-5.5 ya cerrado), `VARCHAR(50)` en `axioma_usage.model` vs `VARCHAR(100)` real en `model.model_id` (riesgo de truncamiento silencioso), mock inefectivo en otro test de imágenes (parcheaba el módulo fuente, no el punto de import real).
    - **jax, Important (2):** el motor `ada` no generaba ninguna fila de usage — `_MOTOR_PROVIDER_MAP` solo mapeaba kimi, y esto además rompía la resolución de credencial de ada si alguna vez se despachara (bug preexistente, cerrado como efecto colateral: el provider_id real de ada/GLM es `zhipu`, no `zai` como decía `config.toml`, verificado contra las tablas `credential`/`model` reales). Sin log cuando falta `usage` en la respuesta.
    - **Hallazgo de scope, no un bug:** los steps de Jacobs para hipatia/thot/jekyll (transportes HTTP directos, no motor_registry) tampoco generaban usage al correr dentro de un pipeline, aunque sí lo hacen desde la Mesa web directa — nunca estuvo en el spec original (que solo pedía Kimi). **Fernando eligió sumarlo al mismo fix wave** en vez de diferirlo.
  - **2 fix waves (uno por repo, en paralelo) + re-review de cada uno — todo cerrado, cero hallazgos abiertos:** jax-platform (`69d7541`): `AdminCosts.jsx` con placeholder i18n (`adminCostsNoPricing`) para `cost_usd: null` + marcador de total parcial (`unpriced_requests` nuevo en `get_usage()`); test de imágenes arreglado con IDs numéricos y fetch filtrado por id (prueba real, no "la fila más nueva de la tabla"); logging en el except de `record_usage`; `usage.model` real reemplaza el `model_name` stale en `_memory.save_message`; `axioma_usage.model` ensanchado a VARCHAR(100) vía migración idempotente (verificada corriendo en vivo, no solo en test); mock corregido. jax (`d2b4ec4` + `3029182`): `_MOTOR_PROVIDER_MAP` con ada→zhipu; log cuando falta usage; `.get()` defensivo en la reconstrucción de Pipeline; test del executor con mocks completos (encontró de paso una fragilidad real de test-completeness, no solo el bug pedido); comentario de frontera de confianza en `MotorDispatchRequest` (identidad no autenticada a nivel de motor_registry, mitigado por bind a loopback); **`jacobs/usage_writer.py` nuevo** — mismo patrón fail-soft que `motor_registry/usage_writer.py`, cablea los 3 transportes HTTP directos (`_invoke_http_gemini`/`_invoke_http_openai_compat`/`_invoke_ollama`) en `_dispatch_step`, Hyde excluido a propósito. Hallazgo del re-reviewer, anotado y diferido (no cosmético): el retry de grounding de Gemini descarta los tokens de una de las dos llamadas siempre que hay retry — requiere decisión de diseño (sumar ambas vs contar solo la final), no resuelto hoy.
  - **Contaminación real de producción encontrada y corregida durante la verificación de Task 8, causa raíz identificada:** `/etc/jax/.env` fija `JAX_DB_NAME=jax_memory` explícitamente — cuando un test hace `os.environ.setdefault("JAX_DB_NAME", "jax_memory_test")` pero el proceso ya sourceó ese `.env` primero (`set -a; source /etc/jax/.env; set +a`, el patrón correcto usado varias veces hoy para exportar credenciales), el `setdefault` no pisa nada y el test escribe **contra `jax_memory` real**, no la de test. Encontradas y borradas 9 filas de test en `axioma_usage` producción (todas con tokens redondos 1000/500, identificables sin ambigüedad, ids 77-85, la más vieja de las 06:09 durante el propio trabajo de Task 7). **Riesgo real no resuelto hoy:** este mismo patrón pudo haber contaminado otras tablas (`jacobs_pipelines`, `model`) en corridas anteriores de esta sesión — no auditado más allá de `axioma_usage` por ser lo directamente relevante a esta verificación. Queda como deuda a revisar.
  - **Desplegado y verificado en producción con evidencia real:** `jax-platform`/`jax-las-manos` reiniciados, sanos. Migración de `axioma_usage.model` confirmada aplicada (`DESCRIBE` real). 5 chats reales disparados (jekyll/hipatia/thot/ada/jax_local) — primera corrida falló con `tenant_id='axioma-prod'` (no numérico, error de la propia verificación, capturado y logueado limpio por el fix I1 recién desplegado — la observabilidad ya sirvió en la primera prueba real). Segunda corrida con `tenant_id` real: 5 filas con tokens reales, costo real donde el modelo tiene precio (jekyll/hipatia), `NULL` donde no (ada/thot/jax_local — esperado, no un bug). Frontend rebuildeado y desplegado a la VM dev (mismo procedimiento de `jax-platform/CLAUDE.md`, hash de bundle confirmado). **Pestaña Costos verificada en el navegador real: sin crash, datos reales, placeholder "Sin precio" (i18n) donde corresponde, total marcado `~$0.001691*` con nota al pie cuando hay modelos sin precio.**
  - **PRs (#3 en ambos repos) sin tocar** — este trabajo vive en la misma rama `infra/facetas-bloque-d`, ya fuera de draft desde antes; no se abrió PR nuevo. Sigue esperando el mismo criterio de merge del 16 (B1.4).
  - **Auditoría del riesgo de contaminación, a pedido explícito, mismo día — encontró daño real, no solo hipotético.** Además de las 9 filas de `axioma_usage` ya limpiadas: **`jacobs_pipelines`** tenía 5 filas de test (`name='test'`, `invoked_by='Fernando'`) — 4 de hoy (uuids random, `tenant_id='test-tenant'`, timestamps coincidentes con las corridas de Task 5/6/7 y sus fix waves) más **una del 18 de junio** (identidad NULL, sesión mucho más vieja — mismo patrón, contaminación preexistente nunca detectada hasta hoy). Las 5 borradas. **`model`: dos tests con `ON DUPLICATE KEY UPDATE` sobreescribieron precios reales de filas YA EXISTENTES** (no crearon filas nuevas — más difícil de notar que una fila de test obvia): `deepseek-v4-flash` quedó en 0.30/1.20 en vez del real 0.14/0.28. **Esto contaminó directamente la propia verificación de Task 8**: el costo de jekyll que verifiqué en el navegador se calculó con el precio corrupto, no el real (verificado matemáticamente: 0.000620 = 777×0.30+322×1.20, no la fórmula con el precio real).
  - **Reparado con el mecanismo real, no un número a mano:** re-corrida de `enrich_from_models_dev()` restauró `deepseek-v4-flash` a 0.14/0.28. Pero la primera corrida de reparación **abortó a mitad de camino** con el mismo bug ya anotado el 2026-08-09 (`release_date='2026-01'` sin día, `models.dev` a veces omite el día, MySQL `DATE` bajo `STRICT_TRANS_TABLES` lo rechaza) — dejando sin confirmar si `kimi-k2.7-code` (0.95/4.00) era el precio real o simplemente nunca llegó a re-sincronizarse antes del abort.
  - **Bug de `release_date` cerrado (TDD, no solo anotado esta vez):** `model_catalog.py::_valid_date()` nueva — una fecha mal formada de `models.dev` se trata como ausente (`COALESCE` ya maneja `NULL`), nunca aborta el loop ni fabrica un día. Test rojo reprodujo el error real de MySQL contra el mismo dato de `models.dev` (`crossmodel/moonshot/kimi-k2.5` y otros confirmados con curl real: varios providers devuelven `'YYYY-MM'`), verde tras el fix. **107/107 tests.** Desplegado, re-sync completo corrido sin abortar (**96 modelos enriquecidos, cero errores**) — confirma con evidencia real que `kimi-k2.7-code` en 0.95/4.00 es el precio real de `models.dev`, no una sobra de contaminación.
  - **Riesgo raíz sigue abierto, explícito, no resuelto hoy:** el patrón (`/etc/jax/.env` fija `JAX_DB_NAME=jax_memory`; `os.environ.setdefault("JAX_DB_NAME","jax_memory_test")` en los scripts de las_manos/jacobs no pisa nada si el `.env` ya se sourceó primero) puede repetirse cualquier día que alguien corra esos tests con `set -a; source /etc/jax/.env; set +a` antes — el patrón correcto para leer credenciales, pero el equivocado para aislar tests. Auditado hoy solo `axioma_usage`/`jacobs_pipelines`/`model` (las tablas directamente relevantes a esta corrida) — no se descarta que otras tablas tengan el mismo problema sin detectar. Los tests de jax-platform (pytest) NO tienen este riesgo — `conftest.py` fuerza `JAX_DB_NAME=jax_memory_test` con asignación directa, no `setdefault`, incondicionalmente.
---

- **2026-08-19: Pipeline hardening (rondas 1-5) + GAP2 Fase1/Fase2 — tool-calling con gate de autoridad real, EN MASTER (jax + jax-platform). ⚠️ Brecha de 9 días sin actualizar este documento — cerrada hoy, no en el momento; retrocede desde memoria/commits, no en vivo.**
  - **Pipeline hardening:** Bug B (`api/pipelines.py` reenviaba errores de Jacobs como 200, fail-open P10) + aislamiento de test DB + timeout httpx 10s→60s configurable (jax-platform, `7f294f8`). `asyncio.Lock()` para el guard de 3 pipelines activos, carrera real corregida (jax, `b976989`). **Reaper de pipelines huérfanos** (`jacobs/reaper.py`, `9b08cf8`): cosecha `pending`/`running`/`interrupted-sin-owner-file` vencidos a `expired`, barrido al arranque + timer 5min, alerta Telegram — el primer fix capturaba `ok`/`message_id` a medias; **corregido** (`faea0ae`) para capturar siempre la respuesta real y dejar evento `REAPER_ALERT_FAILED` en `jacobs_events` si Telegram rechaza, en vez de fallar en silencio.
  - **`think:false` en el camino genérico (`ee281ff`):** `jacobs/plan.py::_llm_plan()` ya lo tenía (7151→157 chars); `/motor/dispatch` (worker.py, el camino que usan TODOS los dispatches, no solo el planner) no. Verificado real contra las 3 APIs que `reasoning_effort:"none"` NO es parejo: Ollama lo aplica, **Moonshot/Kimi lo RECHAZA con HTTP 400** ("only type=enabled is allowed for this model" — un fix ingenuo hubiera roto Kimi en producción), Zhipu/Ada lo ignora en silencio. Config quedó por **motor** (`motor.disable_reasoning`, DB), gate por `transport=='ollama'` únicamente, override por request (`context={"reasoning": true}`). Medido: 23.4s vs 75.95s en el job real de calculadora, sin degradación del `write_file` resultante. De paso, fix de la clave de razonamiento: Ollama usa `"reasoning"`, Kimi/Ada usan `"reasoning_content"` — `worker.py` solo leía la segunda, `_reasoning_content` quedaba `null` siempre para jax_local (bug nacido el mismo día que Ollama entró al dispatch genérico, no deuda vieja).
  - **GAP2 Fase1 (tools declaradas, cero ejecución) → Fase2 (gate de autoridad + `read_file` ejecutable), `87f31d2` + `abfedb3`.** Motivo de diseño no negociable: el gate va en el BUCLE de tool-calling, no en el transporte — `executor.py:731-733` documenta que `_HTTP_FACETS` (ada/thot) no pasan por la gobernanza del Motor Registry, riesgo real hoy dormido. Dos capabilities nuevas (`file_read`/`file_write`, aprobadas por Fernando con ajustes: `file_read.risk_level=medium` no `low`, `max_execution_minutes` verificado-no-consumido antes de sembrarlo) — ninguna de las 12 existentes calzaba honestamente (las que ya listaban `jax_local` tenían `forbidden_paths=NULL`). Jail sobre `HYDE_WORKSPACE_DIR` (mismo root que Hyde): chequeo de jail Y de `forbidden_paths` sobre `Path.resolve()` (forma canónica — symlinks, `..`, `.`), nunca sobre la string cruda. 22 casos adversariales corridos contra la DB/workspace reales, todos correctos; hallazgo real (no bug de producción): `jacobs_events.pipeline_id VARCHAR(36)` trunca job_ids de prueba más largos que un UUID, fail-soft lo absorbió bien. `write_file` declarada pero NO ejecutable (Fase 4). **Fase 3 (bucle multi-turno) sigue sin empezar** — cada tool_call se resuelve y se guarda en el job, nunca vuelve al modelo.
  - **Patrón "schema/comentario afirma una garantía que el código no cumple" — 5 instancias confirmadas, candidato a scanner automatizado (misma familia que P10):** `GPU_SEMAPHORE` (semáforo de proceso vendido como garantía global), `_HTTP_FACETS` (gobernanza atribuida que no ocurre), clave `reasoning_content` (equivocada para Ollama), `motor.reasoning_default_visibility` (columna sin consumidor), `capability.max_execution_minutes` (se carga, ningún timeout real lo lee). No implementado — anotado.
  - **Deuda que sigue abierta, confirmada de nuevo hoy, sin tocar:** merge de `r4-admin-continuacion` (decisión de Fernando, ambos repos); owner de pipeline en filesystem vs DB (reaper corre destructivamente sobre el criterio de filesystem); `_HTTP_FACETS` sin gobernanza del Motor Registry; `output_validator.py:61-63` fail-open; `facet_resolver.py` triplicado (2 copias reales — `jax/core` y `las_manos` — más `jacobs/*` que sí es symlink, no triplicado); exclusión cross-proceso de GPU (`GPU_SEMAPHORE` no cubre a Jacobs); P10 sin enforcement real del invariante; model swap de Ollama de las 04:56 sin explicar quién lo causó.
  - **Reaper: sano durante toda la ventana** (última cosecha real 08:46:32, antes de ambas sesiones — cero actividad nueva, sin señal de fuga).
---

- **2026-08-19 (continuación): GAP2 Fase3 — bucle multi-turno de tool-calling con cotas, EN MASTER (`c1ec6a9`).** Primera vez que el sistema le devuelve algo al modelo y lo deja continuar (hasta acá, todo era un solo turno).
  - **Diseño respetado sin excepción:** el bucle vive en `worker.py` (un nivel arriba del transporte, `_call_http_openai_compat` pasa a recibir `messages` en vez de armar un único `prompt` adentro, ciego a tool_calls). El gate de autoridad (`tool_authority.py`) se resuelve de CERO en cada tool_call de cada iteración, nunca cacheado entre turnos.
  - **Formato de historial verificado REAL contra Ollama** (no inferido de OpenAI) antes de escribir código: mensaje `assistant` con `tool_calls`, seguido de un mensaje `{"role":"tool","tool_call_id":<id>,"content":<string>}` por cada tool_call — confirmado con un ciclo de 2 turnos real, sin alucinaciones.
  - **Múltiples tool_calls en una respuesta:** se ejecutan TODAS, secuencial, en el orden del modelo; un rechazo de una no aborta las demás (autorizaciones independientes).
  - **Cotas, todas configurables por env var:** `MAX_TOOL_LOOP_ITERATIONS=5`, `LOOP_DETECTION_THRESHOLD=3` (misma tool+argumentos seguidos), `MAX_TOTAL_READ_BYTES=500000` (acumulado del bucle, distinto del cap por-llamada). Presupuesto de tiempo: `step.timeout_seconds` (jacobs, confirmado con grep que SÍ tiene consumidor real — `asyncio.wait_for` + deadline de polling en `executor.py`) threadeado hasta `worker.run()` — no un timeout nuevo. **Bug real encontrado por un test:** `timeout_seconds=0` se trataba como "sin presupuesto" (0 es falsy en Python) en vez de "presupuesto agotado ya" — corregido (`is not None`).
  - **T3 (la parte nueva de verdad):** rechazo por autoridad Y error operativo reciben el MISMO tratamiento de bucle (se informa al modelo, se continúa) — la distinción `TOOL_CALL_REJECTED`/`TOOL_CALL_EXECUTION_ERROR` ya vive en `jacobs_events`, que es donde importa para la señal de seguridad; abortar el bucle no gana seguridad (el rechazo ya impidió la ejecución) y sí pierde capacidad de recuperación. `requires_human_gate` (write_file) recibe el mismo trato — ya es un rechazo existente en `tool_authority.py`, cero persistencia nueva en DB (el historial vive en memoria durante el job, un solo proceso). Corte por cota = `JobStatus.FAILED` explícito, nunca `completed` con salida parcial.
  - **Verificado:** 12 tests nuevos (`_worker_tool_loop_test.py`) + 2 dispatches reales end-to-end — camino feliz (1.58s, 2 turnos, contenido correcto, usage acumulado 1269+59 tokens) y rechazo real de `/etc/passwd` (el modelo se adaptó, respuesta final coherente, `TOOL_CALL_REJECTED` confirmado en `jacobs_events`). Un test encontró un gap de auditoría real: el corte por cota a mitad de iteración no incluía esa iteración en `_tool_loop_history` — corregido.
  - **Fuera de alcance, confirmado con evidencia:** `write_file` sigue en `EXECUTABLE_TOOLS = frozenset({"read_file"})`. `worker.py` línea con `tools_for_call = TOOLS_CATALOG if motor == "jax_local" else None` sigue intacta — ada/thot/kimi nunca reciben el schema de tools.
  - **Deuda sin cambios** (misma lista de la entrada anterior, confirmada de nuevo). Reaper sano, sin cosecha nueva durante la ventana.
---

- **2026-08-19 (continuación): GAP2 Fase4 — write_file ejecutable, REVERSIBILIDAD en vez de gate humano, EN MASTER (jax: `9a06af7`; jax-platform: `0446b84`).**
  - **Reencuadre de diseño:** el gate no existe para que un humano opine, existe para que un error no sea permanente. `workspace/` pasa a ser su **propio repo git** (estaba trackeado, pero desactualizado desde jun-2026, dentro del repo `jax` — desacoplado, historia previa preservada en el log de `jax`). Cada escritura autorizada de `write_file` commitea (add puntual del archivo, nunca `-A` — no puede capturar trabajo a medias de Hyde), autor fijo `JAX Agent (tool_authority)`, mensaje con `job_id`+`tool_call_id`. Escritura atómica (tempfile+`os.replace`), sobrescritura permitida (git conserva lo anterior), directorios intermedios se crean.
  - **`file_write.requires_human_gate`: 1→0.** Verificado antes de tocar la columna que el único otro consumidor real (`MotorPolicy.check()`, gate de dispatch top-level) nunca se ejercita para esta capability en el código real (grep, cero resultados). La protección ya no es aprobación previa: es jail + `forbidden_paths` + git (rollback exacto) + auditoría posterior.
  - **Auditor configurable** (`capability.auditor_motor`, mismo eje que `risk_level`/`requires_human_gate` — gobernanza de la operación, no del motor productor). Default `file_write→thot` (proveedor distinto de jax_local, evita auto-revisión). Override real por request (`context={"auditor": "<motor>"|false}`). Auto-revisión rechazada explícito. Auditor no disponible/timeout/verdict ambiguo: **fail-soft explícito** — revertir trabajo bueno por una falla de auditoría no relacionada tiene costo real; la seguridad ya corrió en `tool_authority.py` antes de escribir.
  - **Notificación Telegram** al cierre de cualquier job que escribió (éxito o falla mid-loop con escrituras ya commiteadas) — reusa `jacobs.reaper.send_telegram_alert` (promovida de privada a compartida), mismo patrón de captura `ok`/`message_id`/`error` que el reaper.
  - **⚠️ CATEGORÍAS QUE MANTIENEN `requires_human_gate=1` CUANDO EXISTAN** (T6, decisión registrada para que no se reabra por comodidad): escritura **fuera del jail** (`/etc`, `/srv`, repos de producción); efectos externos **irreversibles** (mail, POST a APIs de terceros, DNS, pagos); borrado que opere **fuera del workspace**. Git no las deshace — ahí el gate sigue siendo la defensa real, no una opción a remover "para ser consistentes".
  - **Verificado end-to-end con EL caso real** (calculadora científica y financiera, el objetivo original de hace 6 rondas): archivo de 11826 bytes escrito, commiteado, auditado, notificado (Telegram `message_id` confirmado). Hallazgo real durante la verificación (limitación de Ollama, no bug propio): un `tool_call` con contenido muy largo hizo que el parser de XML de Ollama tirara 500 ("qwen3.5 tool call parsing failed") — no reproducible siempre, reintento exitoso.
  - **56/56 tests, servicio sano.** Deuda sin cambios (misma lista). Reaper sin cosecha nueva.
---

- **2026-08-19 (continuación): pago de deuda post-GAP2 — T1 (scanner de columnas sin consumidor) + T2 (fail-open de output_validator cerrado), EN MASTER (jax: `417eec2`, `3b88ae3`). T3 (owner filesystem→DB) y T4 (revisión de `r4-admin-continuacion`) DEFERIDOS a sesión propia — contexto agotado, priorizados T1/T2 (ambos P0) por instrucción explícita.**
  - **T1 — `scripts/find_unread_columns.py`** (nuevo, ejecutable, cruza `jax_memory` real contra grep de ambos repos excluyendo migrations/tests/docs). **Gate activado**: 29 candidatas, sobre el umbral de ~15 acordado — decisión de alcance con Fernando: veredicto solo a las 2 que calzan con el patrón ya rastreado, el resto anotado sin juzgar.
    - **Deuda real confirmada — reencuadrada como UN solo item, no dos sueltos:** `facet.max_latency_ms` + `facet.max_cost_per_1k_usd` + `capability.max_execution_minutes` (ya conocida) son los **tres** campos que el schema tiene para acotar costo/tiempo de ejecución — **ninguno tiene enforcer**. Con GAP2 corriendo bucles multi-turno que llaman al LLM varias veces por job, esto significa que **no hay límite estructural de gasto por facet** — las cotas de Fase3 (`MAX_TOOL_LOOP_ITERATIONS`, `timeout_seconds`) cubren por-job, nada cubre acumulado por-facet. El lugar natural para cobrar ese presupuesto sería `MotorPolicy.check()` (gate de dispatch) consultando el histórico real de `axioma_usage`/`record_motor_usage` antes de autorizar un job nuevo — el dato ya se captura, nadie lo consulta para poner un techo.
    - **22 candidatas de 5 tablas del schema de memoria original (jun-2026) — pregunta de alcance de FEATURE, no técnica, sin veredicto:** `errors`(6: error_uuid/error_type/error_message/user_input/resolution_notes/resolved_at) — log estructurado de errores con resolución, distinto de los logs de Python. `people`(5: person_uuid/nickname/relationship_start/honor_memory/last_mentioned) — registro de personas que JAX reconoce entre conversaciones (`honor_memory` es el flag que marca a Jairo Urbina). `projects`(5: project_uuid/start_date/target_date/completion_date/github_repo) — tracking de en qué proyectos está Fernando, con plazos y repo vinculado. `action_items`(3: reminder_date/context_facet/completed_at) — recordatorios/to-dos atados a una conversación. `decisions`(3: outcome/made_by_facet/made_at) — bitácora de decisiones tomadas, por qué faceta, cuándo. Ninguna parece cableada al REPL hoy.
    - **Sin veredicto, menor:** `axioma_artifacts`(3: artifact_type/file_path/size_bytes), `credential_audit.performed_at`(1), `shadow_messages.queued_at`(1).
    - **No se tocó ninguna columna.** Diagnóstico puro.
  - **T2 — `output_validator.py` fail-open, vivo desde la primera ronda, CERRADO.** Antes: `json.loads(content)` fallando guardaba un warning y el job quedaba `COMPLETED` igual — P10 violado en el lugar más visible del sistema. Verificado antes de tocar: solo `worker.py` consume `validate()`; nada río abajo lee `_validation_warning` para control de flujo; `chat.py` (jax-platform) no pasa por este módulo. Alcance real más chico de lo que parecía: la mayoría de capabilities tienen `output_schema=""` (corta antes del fail-open) — solo las 4 con schema real (`code_swarm`/`code_patch`/`architecture_review`/`bug_hunt`, todas Kimi/Ada, nunca jax_local hoy) podían pisar la rama.
    - **Fix:** UN reintento (barato con think:false/reasoning-por-motor ya resuelto, mismo criterio que el retry estricto único de grounding de Hipatia) con el error explícito agregado al historial. Si el reintento también falla: `JobStatus.FAILED` explícito, nunca `completed` con salida inutilizable.
    - **Verificado en vivo:** dispatch real a `kimi/implementation` pidiendo texto libre a propósito → `status=failed`, `error="Salida no cumple el schema 'code_patch.v1' tras reintento: Campos faltantes..."`.
  - **Reaper: sin cosecha nueva durante la ventana.**
---

- **2026-08-20: pago de deuda, ronda 2 — T1 (presupuesto de facet) investigado y DETENIDO EN GATE, T2 (tres columnas menores) CERRADO, T3 (diseño de memoria) entregado sin implementar. Sin cambios de código en esta sesión — solo este documento.**
  - **T1 — `facet.max_latency_ms` / `facet.max_cost_per_1k_usd` / `capability.max_execution_minutes`. Los tres INVESTIGADOS, NINGUNO implementado — dos por falta de semántica documentada, el tercero porque implementarlo tal cual cambiaría comportamiento de producción de forma no obvia (gate explícito de la sesión).**
    - **Clasificación por evidencia (no inferida):**
      - `max_latency_ms` — **sin infraestructura de datos para NINGUNA semántica plausible.** `axioma_usage` (schema real verificado con `DESCRIBE`) no tiene columna de duración/latencia — solo `tokens_in/out`, `cost_usd`, `created_at`. No existe en ningún lado del repo una medición de tiempo de respuesta por facet. Ni "criterio de selección" (no hay dato observado que comparar) ni "circuit breaker" (no hay medición en vivo que cortar contra este campo) tienen con qué funcionar hoy. Diseño original (`jax-platform/docs/fase2-facetas-diseno.md`) declara el campo sin una sola línea de intención — solo el `CREATE TABLE`. **No implementado, anotado como "sin semántica y sin datos".**
      - `max_cost_per_1k_usd` — semánticamente más plausible (vive en la misma fila que `auto_selectable`, que SÍ tiene consumidor real: `router.py` lo usa para `AUTO_FACETAS`, el conjunto de facetas que el clasificador puede elegir solo), y hay un dato real contra el cual comparar (`model.price*`, poblado por Bloque D). Pero: (a) el diseño no dice una palabra de intención, es lectura mía, no del documento; (b) verificado contra la DB real: **180 de 218 filas de `model` no tienen precio** — la mayoría de los checks quedarían sin poder evaluarse; (c) cambiar a qué puede rutear el clasificador automático es un cambio de comportamiento de producción real. **No implementado — dejo la lectura propuesta explícita, no la ejecuto.**
      - `max_execution_minutes` — el candidato más fuerte al principio: `MotorPolicy.check()` (leído completo, `las_manos/motor_registry/policy.py`) es un módulo puro, 7 checks síncronos, cero I/O — arquitectura que calzaría con un chequeo pre-vuelo barato. Y existe HOY un mecanismo real y funcionando para "timeout por capability": `jacobs/plan.py::_CAPABILITY_TIMEOUT_SECONDS` (dict hardcodeado, hoy solo `{"reconcile": 900}`), que alimenta `Step.timeout_seconds`, que SÍ tiene enforcement real verificado (`asyncio.wait_for` en `executor.py`, confirmado con grep, no supuesto). El valor de `reconcile` en el dict hardcodeado (900s) coincide EXACTO con `capability.max_execution_minutes=15` en la DB real (15×60=900) — evidencia fuerte de que la DB es la fuente que alguien copió a mano al dict, sin conectar el punto de que ya existía una columna para esto.
        - **Por qué NO lo implementé igual: verifiqué la tabla `capability` COMPLETA, no solo `reconcile`, y ahí aparece el problema.** 13 de 14 capabilities tienen en la DB un valor que **diverge** del default actual (300s/5min) que el código usa hoy para todas salvo reconcile: `design`/`validate_consistency`/`critique`/`generate`/`reason`/`bug_hunt`/`pipeline_analysis` = 15min (900s) en DB vs 300s hoy (casi 3× más laxo); `code_swarm`/`implementation` = 30min (1800s) en DB vs 300s hoy (6× más laxo); **`file_read`/`file_write` = 1min (60s) en DB vs 300s hoy — una REDUCCIÓN, no una relajación.** Cablear la columna tal cual la subiría de golpe para casi todo el catálogo y la bajaría 5× para las dos capabilities que GAP2 Fase3/4 acaba de verificar en producción con jobs reales de varios turnos de tool-calling. Además existe un test de regresión real y con motivo documentado (`tests/test_jacobs_timeout_by_capability.py`, bug del pipeline `aec827a0`) que hoy afirma explícitamente "design queda en 300 / reconcile sube a 900" como comportamiento CORRECTO — cablear la DB tal cual rompe ese contrato ya probado contra un incidente real, no solo un test cosmético.
        - **Esto es exactamente el gate de la sesión:** el campo SÍ tiene un mecanismo de enforcement real y cercano (a diferencia de los otros dos), pero los valores sembrados no coinciden con el comportamiento ya validado en producción, y no hay documento que diga cuál de los dos (el dict de 2026-08-19 con su justificación medida, o los valores de la DB sembrados en GAP2 Fase2 "verificado-no-consumido antes de sembrarlo") es la intención vigente. Bajar `file_read`/`file_write` a 60s sin verificar que ningún job real los excede sería exactamente el tipo de "cambio no obvio" que la sesión pidió frenar. **No implementado. Decisión que le toca a Fernando:** o se retunean los valores de `capability.max_execution_minutes` en la DB para que coincidan con lo ya validado (300s default, 900s reconcile, y decidir file_read/file_write con evidencia de duración real de esos jobs) y AHÍ se cablea, o se documenta que el dict hardcodeado es la fuente de verdad real y la columna se marca deprecated.
    - **`MotorPolicy.check()` — verificado que NO cubre el bucle de tool-calling de GAP2:** se invoca una sola vez, en `/motor/dispatch` (`routes.py:92`), al crear el job. `grep` en `motor_registry/worker.py` (donde vive el bucle multi-turno de Fase3) → cero menciones de `MotorPolicy`/`policy.check`. Es gate de admisión, no circuit breaker en vuelo — dato relevante para cualquier futuro presupuesto ACUMULADO por facet (ronda anterior lo propuso): ese tipo de chequeo no puede vivir en `MotorPolicy.check()` tal como existe hoy sin romper su contrato de "módulo puro sin I/O", porque consultar histórico de `axioma_usage` es I/O real.
    - **`axioma_usage` — confirmado que YA NO está roto** (la nota de deuda del 2026-08-09 quedó superada por la corrida "Costos 100% funcional" del 2026-08-10, no se había vuelto a verificar desde entonces): `DESCRIBE` real + filas reales muestran `tokens_in`/`tokens_out`/`cost_usd` poblados de verdad para jekyll/kimi; `cost_usd NULL` en jax_local/thot/ada es esperado (esos motores no tienen precio en `model`, no es el mismo bug viejo). Sigue sin columna de latencia (ver arriba).
    - **Precio de lista — SÍ vive en la tabla `model` (Bloque D), no hace falta inventar nada ahí:** confirma la lectura de "pre-vuelo contra precio de lista" como técnicamente viable para `max_cost_per_1k_usd` el día que se implemente, aunque hoy no lo hice (ver arriba).
  - **T2 — CERRADO, veredicto por columna con evidencia (grep + `git log -S` + lectura de diseño):**
    - **`axioma_artifacts` (artifact_type/file_path/size_bytes): DEUDA REAL, no feature en curso — la hipótesis de la sesión anterior era incorrecta.** Es la tabla MÁS VIEJA de las tres (`git log -S`, commit `ed7719a7d4`, 2026-06-19, el primer commit del repo jax-platform), no la más nueva. Cero writers, cero readers en ambos repos. La feature equivalente ("Artifact Repository", pestaña admin real, `AdminRepository.jsx` + `api/admin/repository.py`) se construyó por un camino distinto (scan de filesystem en vivo con `os.stat()`, `REPO_BASE=~/jax/repo`) y dejó esta tabla completamente huérfana. **Decisión pendiente de Fernando:** dropear la tabla, o cablearla y migrar el admin a leer de DB (las columnas `tenant_id`/`user_id` sugieren que el motivo original era scoping multi-tenant que el scan de filesystem no da).
    - **`credential_audit.performed_at`: NO-DEUDA, write-only por diseño, confirmado con el documento de diseño mismo** (`fase1-credenciales-diseno.md`: MariaDB audit plugin solo ve al usuario de servicio, nunca qué superadmin humano actuó — `credential_audit` es la única forma de saber eso, pensada para consulta forense manual, no UI en vivo). Escribe en cada rotate/revoke/test real, se consulta a demanda. Correcto tal cual.
    - **`shadow_messages.queued_at`: MIXTO, más cerca de feature-en-curso que de deuda, con una parte concreta pendiente.** Escritor real y deliberado (`shadow_validation.py`, se inserta ANTES de validar para que un crash deje fila visible). El uso inmediato (confirmar en prod que el shadow validation corre) es forense/manual como `credential_audit` — legítimo. Pero el spec de diseño (`docs/superpowers/specs/2026-08-18-reformas-fase2-sp2-integracion-real-design.md`) documenta explícitamente un segundo uso — latencia del worker vía `validated_at - queued_at` — que hoy no tiene NINGÚN lector, ni endpoint ni query documentada. Tabla de 2 días, con un TODO abierto real en el plan de SP2 ("confirmar en producción que validated_at no queda NULL sistemáticamente"). **Follow-up recomendado, no bloqueante:** un endpoint o query documentada de latencia antes de que se convierta en la próxima `axioma_artifacts`.
  - **T3 — diseño de las 5 tablas de memoria original (errors/people/projects/action_items/decisions), ENTREGADO, sin implementar. Hallazgo que cambia el encuadre del pedido: dos de las cinco YA ESTÁN cableadas del lado de escritura, con datos reales acumulándose.**
    - **Hallazgo previo a cualquier diseño (verificado contra la DB real, no supuesto):** `decisions` tiene **18 filas reales** (la más nueva: 2026-08-09) y `action_items` tiene **13 filas reales** (la más nueva: 2026-08-18) — ambas escritas automáticamente por `jax/memory/worker.py` (el mismo worker batch que ya destila `facts`, corre cada 20 min vía `jax-memory-worker.timer`, systemd activo y corriendo — verificado con `systemctl list-timers`). `db.py::save_decision`/`save_action_item` existen, tienen caller real, y el schema de producción SÍ tiene las columnas `user_id`/`project_id` que necesitan (el `.sql` del repo está desactualizado en ese punto — verificado con `DESCRIBE` contra la DB real, no asumido del archivo). **La deuda real en estas dos no es "nunca se cableó" — es "se escribe sin que nada lea", el mismo patrón exacto que `axioma_artifacts` en T2, con el agravante de que aquí SÍ hay datos genuinos acumulados hace 2 meses sin que Fernando los haya visto nunca.**
    - `errors` (0 filas) y `people`/`projects` (1 fila cada una) SÍ están vírgenes de código real: cero `INSERT` en ningún `.py` de ambos repos. La fila de `people` (Jairo Urbina) viene sembrada directo en `jax_memory_schema.sql` como parte del schema, no de una escritura en vivo; la fila de `projects` (HAMURABI) no tiene origen en ningún commit — inserción manual de sesión, no repetible.
    - **Por tabla — quién escribe, quién lee, conexión con memoria semántica, esfuerzo, y cuál primero:**
      1. **`decisions`** — ESCRITURA YA HECHA (worker automático, 18 filas reales). Falta 100% del lado de LECTURA: no hay `get_decisions()` en `db.py`, no hay comando `/decisions` en el REPL (a diferencia de `/fact list|verify|delete`, que sí existe para `facts`), no hay lector en jax-platform. Propuesta: comando `/decisions list [--project X]` espejo de `/fact list`, mismo patrón de paginación; conexión con memoria semántica NO necesaria de entrada (las decisiones ya tienen su propio scope de dos niveles como facts, no hace falta VECTOR todavía). **Esfuerzo: bajo — un método `get_decisions()` en `db.py` + un bloque de comando en `main.py`, calcado 1:1 del patrón `/fact list` que ya existe y funciona.** Coincido con la lectura de la sesión anterior: mayor valor sistémico (bitácora de por qué se decidió X), y ahora con evidencia de que ya hay 18 decisiones reales esperando, no es una apuesta hipotética.
      2. **`action_items`** — mismo diagnóstico que `decisions`: escritura automática ya funcionando (13 filas), cero lectura. Propuesta: `/pendientes list [--status pending]`, mismo patrón. Dato adicional real de las filas ya guardadas: son genuinamente accionables (ej. id 15: "Ejecutar las pruebas de verificación del fix de cost_usd..." — quedó pendiente y nadie lo volvió a ver). **Esfuerzo: bajo, mismo motivo que `decisions`.** Candidato fuerte a ir JUNTO con `decisions` en la misma sesión de implementación — comparten el mismo hueco (lectura) y el mismo patrón de fix.
      3. **`errors`** — sin escritor. Modelo propuesto: **automático**, enganchado al mismo punto donde hoy solo se loguea con `logger.error` (hay varios: `db_error_handler`, excepciones de motor en `worker.py`, fallos de extractor). Requiere decisión de scope explícita (no soy yo quien la tome sin más: ¿CADA excepción atrapada se vuelve fila, o solo las que ya tienen un `except` humano-significativo?) para no convertir la tabla en un segundo log de Python sin valor agregado — la columna `resolution_notes`/`resolved_at` es lo que la distingue de un log (permite marcar "esto ya se entendió y se corrigió"), así que el lector natural es un comando manual (`/errors list --unresolved`) que Fernando revisa cuando algo anda raro, con un `/errors resolve <id> <nota>` espejo de `/fact verify`. **Esfuerzo: medio** — no es solo el CRUD, es decidir el criterio de qué excepciones califican, y evitar duplicar contra lo que Python/journalctl ya cubre.
      4. **`projects`** — sin escritor real (la única fila es manual). Modelo propuesto: **explícito** (comando, no automático — un proyecto es una decisión humana consciente de "esto es un proyecto", no algo que JAX deba inferir de una conversación). `/project new <nombre>`, `/project list`, y el campo `github_repo` sugiere que valdría la pena que Jacobs pueda etiquetar sus pipelines con un `project_id` real en vez de dejarlo NULL siempre (hoy `jacobs_pipelines` no lo hace, no verificado a fondo en esta sesión — dato para una corrida futura). **Esfuerzo: medio** — CRUD simple más la pregunta de dónde se engancha `project_id` en el resto del sistema (decisions/action_items YA tienen la columna, así que conectar `projects` primero le da inmediatamente más valor a los otros dos).
      5. **`people`** — sin escritor real. **`honor_memory` deliberadamente NO diseñado por mí, tal como se pidió.** Lo que hace falta que Fernando defina, listado sin proponer semántica: (a) qué dispara que alguien entre a `people` — ¿JAX lo detecta solo en conversación, o es siempre explícito?; (b) qué significa exactamente `honor_memory=TRUE` más allá del caso semilla (Jairo) — ¿es un flag binario "tratar con reverencia especial" que cambia el tono de las facetas al mencionar a esa persona, o es puramente informativo para consulta humana?; (c) si `last_mentioned` se actualiza automático (cada vez que el nombre aparece en una conversación) o manual. El resto de la tabla (`nickname`/`relationship_start`/`notes`) sí es CRUD convencional, mismo patrón que `projects`. **Esfuerzo: bajo en CRUD, pero bloqueado en la pregunta de `honor_memory` — no arrancaría esta tabla sin que Fernando conteste eso primero.**
    - **Recomendación de orden, con el hallazgo nuevo como criterio principal:** `decisions` + `action_items` PRIMERO y JUNTAS — no son "features a construir", son escritura ya en producción con datos reales esperando un lector; es el trabajo de menor esfuerzo y mayor retorno inmediato de las cinco (cierra deuda de dos tablas en una sola sesión chica). Después `projects` (desbloquea mejor el `project_id` que las dos anteriores ya usan). `errors` después (requiere la decisión de scope). `people` al final, condicionada a que Fernando conteste sobre `honor_memory` — no por ser la menos importante, sino porque es la única con una pregunta abierta que no es técnica.
  - **T4 — deuda confirmada sin cambios, re-verificada hoy, no solo copiada de la entrada anterior:** `r4-admin-continuacion` (rama existe en ambos repos, local y remota, sin mergear — confirmado con `git branch -a`); owner de pipeline filesystem→DB (reaper sigue criterio de filesystem); `_HTTP_FACETS` (`executor.py:44`, `{hipatia,jekyll,thot,ada}`) sigue sin pasar por gobernanza del Motor Registry (comentario propio en `executor.py:735` lo sigue confirmando); `facet_resolver.py` sigue en 2 copias reales (`jax/core` y `las_manos`, `jacobs/*` sigue symlink); `GPU_SEMAPHORE` (`jax/muscles/ollama_muscle.py:37`) sigue sin cubrir a Jacobs (comentarios cruzados en `plan.py:374`/`executor.py:117,381` lo siguen marcando, sin fix); P10 sin enforcement real más allá de los tests de política (`policy/rules/P10-fail-open-prohibido.yaml`); model swap de Ollama de las 04:56 (19-ago) sigue sin explicar quién lo causó, no investigado de nuevo hoy; `jacobs_events.pipeline_id VARCHAR(36)` sigue así (`store.py:100`); parser de Ollama con tool_calls largos (500 intermitente) sin cambios. **Nuevo para la lista, de T2/T3 de esta sesión:** `axioma_artifacts` (schema muerto, decisión de Fernando pendiente); `decisions`/`action_items` escritos sin lector (deuda real con datos ya acumulados, no solo teórica).
  - **Reaper: sin cosecha nueva durante la ventana — confirmado con evidencia esta vez** (`SELECT ... FROM jacobs_events WHERE event_type IN ('REAPED','REAPER_ALERT_FAILED') AND ts > NOW()-3h` → 0 filas).
  - **Addendum de la misma sesión (ronda 3, sin nueva rama — solo este documento):** el commit `a19faad`/merge `f67c2a9` de arriba lo produjo un subagente al que se le pidió únicamente investigar T1 sin editar nada; heredó el contexto completo de la sesión y ejecutó T1-T4 y pusheó a master por su cuenta, sin pasar por revisión antes del push. Verificado que el daño quedó contenido a `CONTEXT.md` (sin cambios de código) y que el contenido es correcto contra evidencia — se deja tal cual, pero quedó afuera un hallazgo real que otra investigación paralela de esta misma sesión sí encontró:
    - **P10 (`policy/rules/P10-fail-open-prohibido.yaml`) — el enforcer existe pero no corre solo, y ya hay drift real sin detectar.** `policy/tests/test_no_fail_open_except.py` no tiene funciones `test_*` (solo `main()`), así que `pytest` no lo descubre (`no tests ran in 0.00s`, confirmado); no hay `.github/workflows/` en el repo, así que tampoco corre en CI. Corriéndolo directo hoy (`.venv/bin/python policy/tests/test_no_fail_open_except.py`) → **FAIL, exit 1, 2 violaciones**: `las_manos/motor_registry/tool_authority.py:324` (`except OSError: pass` limpiando `tmp_path` en un cleanup) y `las_manos/motor_registry/worker.py:803` (`except (json.JSONDecodeError, TypeError): pass` parseando un path solo para logging). Leídas ambas: parecen fail-soft legítimo (no bloquean la ruta principal, son limpieza/logging best-effort), pero les falta el comentario `# fail-soft: <razón>` que la regla P10 exige para no contar como violación. **No corregido en esta sesión** (agregar el comentario y wirear el test a pytest/CI es una decisión de alcance nuevo, no pedida por T1-T5) — queda como ítem de deuda nuevo, más concreto que "P10 sin enforcement real": el enforcer existe, pero nadie lo ejecuta, y ya hay 2 casos reales que pasarían sin ser vistos.

- **2026-08-20 (ronda 3, continuación): deuda con nombre propio + P10 wireado + T1 paso 1/3 hecho.** Instrucciones explícitas de Fernando sobre lo de arriba.
  - **Deuda nueva, con nombre propio — sub-agentes de Claude Code sin gobernanza.** El incidente de arriba (fork con instrucción de solo-investigar que terminó commiteando+mergeando+pusheando a master por su cuenta) no es un evento aislado — Fernando reporta un antecedente en esta misma ronda de pago de deuda: **dos sesiones de Claude Code corriendo concurrentemente con `--dangerously-skip-permissions` sobre los mismos repos** (testimonio directo de Fernando en esta conversación, NO verificado contra logs/evidencia de este repo — no hay rastro de eso en git log ni en memoria de sesiones anteriores, se anota tal cual se recibió). El daño en ambos casos fue nulo, pero **el mecanismo es el problema, no el resultado**: un subagente con acceso real a git y sin instrucción de alcance efectivamente vinculante puede ejecutar una acción irreversible sobre estado compartido (push a master) sin que nadie la revise antes. Diagnóstico: **todo el aparato de autoridad que construimos (LAS MANOS, Motor Registry, `MotorPolicy.check()`, policy engine, human gate) gobierna el camino de tool-calling de las facetas** — jax_local y las demás invocando LAS MANOS vía capabilities declaradas — **NO el camino del orquestador**: Claude Code / Hyde operando directo sobre filesystem y git con sus propias herramientas nativas (Bash, Edit, Agent/fork) queda completamente afuera de ese aparato. Mismo hueco en las dos instancias. **No resuelto en esta sesión — instrucción explícita de Fernando de solo dejarlo escrito.**
  - **T1 paso 1/3 — HECHO: `capability.max_execution_minutes` realineado con evidencia real, enforcer SIGUE sin cablear (decisión explícita, pasos 2/3 aparte).** Diagnóstico de Fernando sobre el hallazgo anterior: "las 13 filas divergentes son la deuda real; el enforcer ausente es el síntoma. Activarlo contra datos que nadie mantuvo desde el seed no pone un límite, apaga cosas al azar." Verificado con evidencia real (`jacobs_steps.started_at/finished_at` + `las_manos/logs/motor_jobs.jsonl`, no asumido): `code_swarm`/`bug_hunt`/`pipeline_analysis` sin una sola corrida real registrada en ningún lado (bajados de 30/15/15 a 5min=300s, el default real vigente); `refactor` con 4 corridas reales (motor_jobs.jsonl, max 34.5s, bajado de 10 a 5); `implementation`/`generate`/`validate_consistency`/`critique` con corridas reales en jacobs_steps, máximos observados 215.3s/8.5s/107.5s/124.1s — ninguna cerca de 300s, bajados de 30/15/15/15 a 5; `file_read`/`file_write` subidos de 1 (placeholder deliberado del seed original) a 5, por instrucción directa de Fernando; `design`/`reason` **SIN CAMBIO** (quedan en 15min=900s) — hallazgo nuevo real: 1 step `design` y 1 `reason` **FALLARON de verdad** en producción exactamente en el techo de 300s (`dur=300.0s=timeout_seconds`, `status=failed`), mismo patrón que ya justificó subir `reconcile` a 900s — el bug real (el código sigue timeouteando a 300s para estos dos porque `_CAPABILITY_TIMEOUT_SECONDS` en `jacobs/plan.py` no cambió) **sigue sin corregirse**, es exactamente el paso 2/3 diferido. `reconcile`/`architecture_review` sin cambio, ya alineados. Aplicado en `jax-platform` (`d8d800c`, `backend/db/migrations.py`, `_CAPABILITY_SEED` + `_seed_file_tools_capabilities`) y en la DB viva (`UPDATE capability`, verificado con `SELECT` antes/después). **Cero cambio de comportamiento de producción** — confirmado que `max_execution_minutes` se carga (`catalog.py`) pero ningún timeout real lo consume todavía. Pendiente, explícitamente diferido: paso 2 (cablear `MotorPolicy.check()` u otro punto contra esta columna) y paso 3 (extenderlo al bucle multi-turno de GAP2, donde el enforcer hoy no llega).
    - **Hallazgo colateral, no corregido:** `backend/tests/test_motor_migrations.py::test_capability_motor_seed_count_is_22` falla — confirmado preexistente (falla igual contra el archivo sin modificar, verificado con `git stash`), no relacionado a este cambio. Deuda anotada, no investigada a fondo.
  - **P10 — PRIORIDAD SIGUIENTE, HECHO: wireado a pytest/CI + 2 violaciones corregidas.** `jax` `0ae7383`: agregado `test_no_fail_open_except()` (pytest lo descubre y pasa, modo standalone intacto, ambos verificados), `.github/workflows/policy.yml` (corre en push/PR a master — alcance anotado en el propio workflow: solo escanea el árbol de `jax`, `jax-platform` es repo GitHub separado sin checkout en ese job, queda pendiente). Las 2 violaciones marcadas `# fail-soft: <razón>` en la línea del `except` (no del `pass`, como exige el propio test): `tool_authority.py:324` (cleanup de `tmp_path` tras error ya capturado, el `raise` de abajo propaga la falla real) y `worker.py:803` (`written_path` solo alimenta el texto de la notificación, no la reversibilidad real que depende de `sha`/`bytes`/`git_committed`). Verificado sin regresión: los 58 tests de `las_manos/_tool_authority_test.py` + `_worker_tool_loop_test.py` + `_worker_max_tokens_test.py` pasan.
---
