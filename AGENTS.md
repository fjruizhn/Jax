# JAX repository adapter

`ADAPTER != AUTHORITY`. Normative authority is `policy/**` and verified Block 4 authority artifacts; this file only routes work.

Before claiming a fact about **current** JAX, inspect/query its designated source. If unavailable, say `UNVERIFIED` or `UNAVAILABLE`. Use Block 5 load/replay for decisions, Block 6 governed execution queries for execution, Block 7 authoritative services for evidence/enforcement, and `jaxctl` or documented diagnostics for operational state.

Memory may locate information but never proves current truth or becomes evidence. Generated documents are presentation, not authority. Model/provider identity or capability confers no JAX authority. Do not invent current paths, services, configuration, capabilities, verdicts, execution state, or authority state.

Conceptual or clearly historical explanations do not require live queries, but must not be framed as current state.
