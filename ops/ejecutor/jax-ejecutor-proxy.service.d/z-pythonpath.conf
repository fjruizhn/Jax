# 2026-09-25 (auditoría escalón 3, MAJOR-1 de jax#274): el PYTHONPATH efectivo
# de jax-ejecutor-proxy apuntaba a /home/fruiz/jax -- el checkout de TRABAJO
# de un agente, en rama ajena, no el de producción. Las importaciones tardías
# de este proceso (`from facet_resolver import ...` en
# jax/ejecutor/mision_servicio.py, contratos/vigia_servicio.py y
# contratos/arranque.py:374 -- pruebas_reales(), que además importa
# jacobs.store.conexion y jax.ejecutor.contratos.auditor_cliente/eleccion_c5/
# canario_c1/canario_c3/canario_c5/exportar -- todas dependen de que
# las_manos/ esté en sys.path para el import pelado de facet_resolver) se
# resolvían ahí -- código sin revisar podía terminar corriendo en producción
# sin ningún reinicio de por medio.
# Demostrado ANTES de fijar este valor (ronda 1 y ronda 2 de la auditoría):
# con este mismo PYTHONPATH, `jax.ejecutor.proxy_carril`, `facet_resolver`,
# `credential_resolver`, `db_connect_config`, `jacobs.store` y
# `jax.ejecutor.contratos.auditor_cliente`/`eleccion_c5`/`canario_c1`/
# `canario_c3`/`canario_c5`/`exportar` (todo lo que importan
# mision_servicio.py/vigia_servicio.py/contratos/arranque.py además de
# facet_resolver) importan sin error corriendo como jaxsvc con
# /srv/jax-prod/jax/.venv/bin/python (el mismo intérprete de ExecStart).
[Service]
Environment=PYTHONPATH=/srv/jax-prod/jax:/srv/jax-prod/jax/las_manos
